# План 6b Путь B — миграция легаси-счетов в AccountsCore + снос легаси-миров

**Дата создания:** 2026-07-07 · **Статус:** В РАБОТЕ (Ф1/Ф1.5/Ф2/Ф2b/Ф3/Ф4/Ф5a/Ф5b РЕАЛИЗОВАНЫ на ветке, НЕ мержены; Ф2/Ф3/Ф4/Ф5a/Ф5b — scoped; Ф2c/Ф5c ЗАБЛОКИРОВАНЫ — требуют декомпозиции, см. Ф5c) · **Тип:** L, Bulletproof
**Одобрение:** владелец 2026-07-07 — планирование + фаза 1 реализации (ночной runbook `plans/2026-07-07__overnight-run.md` §5, задача №6/№7).
**Вводная:** приложение стоит у 1–2 реальных пользователей; снос признан безопасным стресс-тестом 2026-07-06; потеря истории легаси при opening-balance-миграции согласована владельцем.
**Вход:** `plans/2026-07-02__accounts-system-audit.md`, `plans/2026-07-04__accounts-core-rebuild-plan.md` (фазы 0–6a реализованы, смержены в develop), `progress/accounts-core-rebuild-handoff.md`, память `millio-6b-path-decision`.

> **Риск №1 (двоемирие legacy/AccountsCore).** Сегодня два мира счетов живут параллельно (Т7/S3 базового плана): легаси `Card/Credit/Investment/FinanceAccount` — read-only с Фазы 6a, новое ядро `Account/AccountEvent` — единственный писатель. Чтение слито в 4 точках (см. §4). Путь B закрывает двоемирие: мигрирует данные легаси в ядро и физически сносит легаси-мир. Это устраняет целый класс багов рассинхрона (три пути тотала, дубли, «плывущая» история), описанных в аудите 2026-07-02.

---

## 1. Инвентарь легаси-файлов (по кодбазе, 2026-07-07)

**Метод:** `grep -rlwE "\b(Card|Credit|Investment|FinanceAccount)\b"`. Итог: **80 файлов app-таргета** + **50 тест-файлов** ссылаются на легаси-типы (union ≈ 130; совпадает с оценкой плана «Card 49 / Investment 41 / Credit 22 / FinanceAccount 25»). Из них **выделенно-легаси, удаляемые целиком — ~26 файлов** (ниже), остальные ~54 app + ~50 тестов — правятся (снятие ссылок) или удаляются точечно.

### 1.1 Models (SwiftData @Model — удаляются целиком)
| Файл | Тип |
|------|-----|
| `millio/UI/Services/CardIndex/Card.swift` | `Card` (@Model) |
| `millio/UI/Services/Credits/Credit.swift` | `Credit` (@Model) |
| `millio/UI/Services/Investments/Investment.swift` | `Investment` (@Model) |
| `millio/UI/Services/Finances/FinanceAccount.swift` | `FinanceAccount` (строковая junction-связь) |

⚠️ `FinanceGroup.swift` — судьба решена ревью 2026-07-09: **консолидация с `AccountGroup` — Фаза 1.5** (см. ниже), не «за скоупом». Снос самого файла `FinanceGroup.swift` откладывается до Фазы 5 (после того как все читатели переключены на `AccountGroup` в Фазе 1.5) — модель числится там же, где остальные @Model-файлы легаси-мира.

### 1.2 Core / регистрация / бэкап-импортёры (удаляются / правятся)
| Файл | Действие |
|------|----------|
| `millio/UI/Services/CardIndex/CardManager.swift`, `CardCatalog.swift`, `CardFeatureRegistration.swift` | снос |
| `millio/UI/Services/Credits/CreditManager.swift`, `CreditFeatureRegistration.swift` | снос |
| `millio/UI/Services/Investments/InvestmentManager.swift`, `InvestmentFeatureRegistration.swift` | снос |
| `millio/UI/Services/Finances/FinanceFeatureRegistration.swift` | **правка** — снять регистрацию `Card/Credit/Investment/FinanceAccount` importer'ов |
| Бэкап-импортёры `CardImporter/CreditImporter/InvestmentImporter/FinanceAccountImporter` (в *FeatureRegistration) | снос **осознанно** — старые бэкапы с легаси-счетами не восстановятся (хвост из памяти, при 1–2 юзерах OK) |
| `millio/Core/AccountsCore/LegacyAccountConverter.swift`, `LegacyConversionRegistry.swift`, `millio/UI/Services/Finances/LegacyAccountConversion.swift` | снос **после** завершения миграции (Track C-инфра, больше не нужна) |

### 1.3 UI (удаляются / правятся)
| Файл | Действие |
|------|----------|
| `CardIndex/CardViewModel.swift`, `Credits/CreditViewModel.swift`, `Investments/InvestmentViewModel.swift` | снос |
| `Finances/CardEditorView.swift`, `CreditEditorView.swift`, `InvestmentEditorView.swift` | снос |
| `Finances/FinanceAccountService.swift`, `FinanceInvestmentOrderService.swift` | снос |
| `Finances/Editors/FinanceCreateViews.swift`, `Editors/FinanceEditorWrappers.swift`, `InlineForms/InlineCreateForms.swift` | снос — подтверждённый мёртвый код (аудит 6a п.6) |
| `Finances/AccountTotalPolicy.swift` | снос — политика включения ТОЛЬКО легаси-счёта в тотал; после сноса не нужна (см. §4) |
| `Finances/Import/StockBulkImportServices.swift`, `StockBulkImportSheet.swift` | **правка/снос** — 🔴 ХВОСТ: ещё создаёт легаси `Investment` (аудит 6a п.7); мигрировать на ядро или снять экран ДО удаления `Investment.swift` (иначе не соберётся) |
| `Core/App/ScreenshotDataSeeder.swift` | правка — легаси-сиды только в `isScreenshotMode` (домен Nova); перевести на ядро или изолировать |
| ⚠️ **Не сносить, только снять ссылки:** `FinanceViewModel.swift`, `FinanceTotalsService.swift`, `FinanceDynamicsViewModel.swift`, `FinanceGroupService.swift`, `FinanceDynamicsView.swift`, `FinancesView.swift`, `FinanceRows.swift`, `Cashflow/*` (мосты, персистенс), `QuickSetupApplier.swift` | правка |
| `Investments/MarketData/*` (10 файлов, вкл. `TwelveDataClient`) | **оставить** — SHARED, `AccountMarketPriceService` (ядро) зависит от `MarketDataClientProtocol`/`MarketAPIClient` |

### 1.4 Tests (~50 файлов)
Легаси-тесты `Card*Tests`, `Credit*Tests`, `Investment*Tests`, `FinanceAccount*Tests` — снос вместе со своими мирами. Тесты общих файлов (`FinanceViewModelTests`, `CashflowViewModelTests`, backup/reconciliation) — правка под single-world. Baseline-падения (16 известных + LanguageManager-flaky) — фиксировать по `progress/accounts-core-baseline-failures.md`, гейт «ноль новых красных».

---

## 2. Порядок сноса по слоям (что от чего зависит, что рвётся первым)

Компиляция ломается сверху вниз при снятии типа, поэтому снос идёт **снизу вверх по зависимостям**:

1. **Сначала — живые писатели легаси** (иначе останутся без замены): `StockBulkImportServices` (создаёт `Investment`), `QuickSetupApplier` (уже на ядре — проверить), `ScreenshotDataSeeder`. Пока они пишут `Investment`, `Investment.swift` не удалить.
2. **Затем — читатели-мосты и total-слой**: снять ссылки на легаси в `FinanceViewModel`/`FinanceTotalsService`/`FinanceDynamicsViewModel`/`FinanceRows`/`FinancesView`/Cashflow-мостах. Здесь рвётся первым `AccountTotalPolicy` + `mergingNewCoreSeries` dual-path (см. §4).
3. **Затем — редакторы/VM/менеджеры**: `*EditorView`, `*ViewModel`, `*Manager`, `CardCatalog`, `FinanceAccountService`, `FinanceInvestmentOrderService`, мёртвый код.
4. **Затем — регистрация и бэкап-импортёры**: `*FeatureRegistration`, importer'ы; снять типы из `ModelTypeRegistry`.
5. **Затем — схема SwiftData**: `Card/Credit/Investment/FinanceAccount` убрать из `AppSchemaCurrent` → **новая версия схемы V6** + lightweight-стадия (таблицы дропаются). Только когда ни один тип больше не референсится.
6. **Последними — сами @Model-файлы** `Card.swift`/`Credit.swift`/`Investment.swift`/`FinanceAccount.swift` + `LegacyAccountConverter`/`LegacyConversionRegistry` (после миграции).

**Что рвётся первым:** `AccountTotalPolicy` и dual-path merge в total-слое (шаг 2) — самая сцепленная точка; поэтому переключение чтения на single-world делается рано и отдельным гейтом.

---

## 3. Миграция данных существующих счетов (1–2 юзера) в AccountsCore

**Инструмент готов:** `LegacyAccountConverter` (`millio/Core/AccountsCore/`) + `LegacyConversionRegistry`. MVP: переносит **opening-balance** легаси-счёта (текущий баланс) в новый `Account`, **без реплея истории** (история легаси теряется — согласовано владельцем при 1–2 юзерах). Атомарно: создаёт core-двойник → `hideLegacy()` ставит `archivedAt`; при сбое — компенсирующий откат (`unconvert`).

**Идемпотентность:** `registry.isConverted(legacyUniqueID:)` — повторный прогон не задваивает. Реестр хранится в **UserDefaults** (осознанно — не поле @Model).

**Где вызывать:** одноразовый оркестратор `LegacyAccountsMigrator` (новый, `@MainActor`) при старте с user-скоупом (после reconciliation Track B, до первого рендера Финансов), под флагом-в-SwiftData «миграция выполнена» (НЕ UserDefaults — урок `DailySnapshotMigrator`, чтобы переносился при restore). Обходит все `Card/Credit/Investment` (не converted), маппит → `Input`, вызывает `convert`.

**🟡 Известный гэп конвертера:** `Input` поддерживает только `cardMeta/loanMeta/manualAssetMeta`. Легаси-**вклад** (`Investment` preset=deposit) и **рыночная** позиция (`Investment` cat=stocks/crypto) мигрируют как `.manualAsset`/`.debitCard` с opening-balance — теряют ставку/капитализацию/тикер. При 1–2 юзерах и согласованной потере истории — допустимо; иначе расширить `Input` на `depositMeta/marketMeta/debtMeta` (не блокер фазы 1, помечено).

**UserDefaults-легаси-снапшоты:** старые дневные снапшоты в UserDefaults (память `millio-accounts-system-audit`) + ключи `LegacyConversionRegistry` — **чистятся одноразово** на финальной фазе (после успешной миграции и сноса), т.к. opening-balance-миграция историю не переносит. Кэш ядра (`AccountDailySnapshot`) пересобирается из событий (`AccountSnapshotRebuilder.rebuildAll`).

---

## 4. Судьба мостов и AccountTotalPolicy

| Компонент | Файл | Судьба после сноса |
|-----------|------|--------------------|
| **AccountsCoreAdditionBridge** | `.../Finances/AccountsCore/AccountsCoreAdditionBridge.swift` | **ОСТАЁТСЯ** (маппит 11 пресетов → kind/meta при создании). Упрощается: ветки `isEditingLegacy` в 4 kind-резолверах становятся мёртвыми → удалить. |
| **AccountsCoreCashflowBridge** | `.../Cashflow/AccountsCoreCashflowBridge.swift` | **ОСТАЁТСЯ**, сильно упрощается: исчезают ветки «оба легаси → no-op», «смешанный старый↔новый перевод», world-switch-квадранты, резолвер `resolveNewCoreAccount` (легаси-коллизия UUID). Перевод сводится к чистому new↔new. |
| **AccountsCoreDepositCashflowBridge** | `.../Cashflow/AccountsCoreDepositCashflowBridge.swift` | **ОСТАЁТСЯ** (материализация вклад→Cashflow, регрессия 2026-07-05). Легаси-веток нет → правок минимум; проверить, что не резолвит `Investment`. |
| **AccountTotalPolicy** | `.../Finances/AccountTotalPolicy.swift` | **СНОСИТСЯ.** Это политика «включать ли ЛЕГАСИ-счёт в тотал и с каким знаком». У ядра эквивалент — `Account.participates(date)` (time-aware, Фаза 5) + знак в движке C. После сноса легаси политике нечего решать. |

**Что упрощается в total-слое:** `FinanceTotalsService.calculateTotalsSnapshot` и `FinanceDynamicsViewModel.calculateTotalForAllGroups` сейчас складывают ДВА вклада (легаси через `AccountTotalPolicy` + ядро через `AccountsTotalsService.totalAt`). После сноса — только `AccountsTotalsService.totalAt`. `ChartDataPoint.mergingNewCoreSeries` (поточечное сложение двух рядов) схлопывается в один ряд `seriesBetween`. **Три пути тотала → один** (закрывает R1 аудита 2026-07-02). Точки чтения легаси на снос/правку: `FinanceViewModel.newCoreAccounts`, `FinanceRows` (dual-render), `FinanceDynamicsViewModel` merge, `mergingNewCoreSeries`.

### 4a. Слияние моделей групп `FinanceGroup` ↔ `AccountGroup` — вход из ревью 2026-07-09

**Диагноз** (`plans/2026-07-05__unified-totals.md` §1.3a, верифицирован повторно 2026-07-09): группы, целиком состоящие из AccountsCore-счетов, показывают сумму 0 / «No finance products» / «No groups» на трёх независимых экранах — все три call site (`FinanceTotalsService.calculateGroupTotal:148-160`, `FinanceGroupService.orderedAccounts(for:):83-88`, `FinanceDynamicsViewModel.hydratedAccounts(in:):2681-2695` + `updateDynamicsBreakdown():1059-1086`) читают только `FinanceGroup.accounts` (legacy junction), не зная про `Account.group` (AccountsCore).

**Подтверждено ревью:** `FinanceGroup` (`.../Finances/FinanceGroup.swift:13-48`) и `AccountGroup` (`Core/AccountsCore/AccountGroup.swift:6-17`) — два независимых SwiftData `@Model`, без FK-связи. Единственный мостик — `AccountsCoreAdditionBridge.resolveAccountGroup` (:64-78): при создании/миграции AccountsCore-счёта ищет `AccountGroup` по имени, создаёт новый, если нет. Это уже используется Фазой 1 (двойник счёта резолвит свою группу по имени junction'а), но **только резолвит/создаёт** — не переносит остальные поля и не является постоянной связью:

| Поле | `FinanceGroup` | `AccountGroup` | Судьба при слиянии |
|------|-----------------|-----------------|---------------------|
| `colorHex` | `String` (non-optional) | `String?` | переносится |
| `displayCurrency` | `String?` | `String?` | переносится |
| `order` | есть | есть | переносится |
| `isFavorite` | есть | **нет** | ✅ переносится (решение владельца 2026-07-09) |
| `usesManualAccountOrdering` | есть | **нет** | ✅ переносится |
| `priorityRaw` | есть | **нет** | ✅ переносится |
| иконка (кастомная) | нет (только вычисляемая по доминирующему типу, `FinanceRows.swift:203-227`) | нет | см. ниже — отдельный вопрос, не блокирует слияние |

**Решение по месту в плане:** это НЕ точечная правка трёх call site (получился бы ещё один временный мост поверх временного) и НЕ часть Фазы 2 (та про total/read-слой уровня счёта, а не про модель группы — разный радиус изменений: тут schema-миграция @Model + смена типа у 3 UI-читателей + редактор группы). Вставляется **новой Фазой 1.5**, сразу после уже реализованной Фазы 1 и **до** Фазы 2 — потому что Фаза 2 (переключение total/read-слоя на single-world) для per-группных сумм физически не может завершиться, пока группы не унифицированы: иначе Фаза 2 просто узаконит тот же баг «AccountsCore-группа = 0» как «ожидаемое поведение».

### 🟢 Фаза 1.5 — Слияние моделей групп (`FinanceGroup` → канон `AccountGroup`) — [x] РЕАЛИЗОВАН (2026-07-09, Александр, ветка `feature/legacy-accounts-purge`, НЕ мержено)

**Что:** `AccountGroup` расширяется недостающими полями (`isFavorite`, `usesManualAccountOrdering`, `priorityRaw` — переносятся все три, решение владельца 2026-07-09); новый одноразовый `GroupsMigrator` (аналог `LegacyAccountsMigrator`, тот же паттерн: идемпотентность через постоянный маркер, не UserDefaults-only) переносит для каждой активной `FinanceGroup` эти поля на резолвленный/созданный `AccountGroup` (переиспользует `resolveAccountGroup`, но делает перенос полей один раз, а не только name-match). Три call site (`calculateGroupTotal`, `orderedAccounts(for:)`, `hydratedAccounts(in:)`/`updateDynamicsBreakdown()`) переключаются читать `AccountGroup`/`Account.group` вместо `FinanceGroup.accounts`. Редактор группы (`FinanceGroupEditorView`) переводится на `AccountGroup`. Сама модель `FinanceGroup.swift` **не удаляется** в этой фазе (это происходит в Фазе 5 вместе с остальными @Model-файлами легаси-мира, после того как ничего её больше не читает) — только читатели переключаются.

**Acceptance criteria:**
- AC1: группа, целиком состоящая из AccountsCore-счетов, показывает верную сумму / список продуктов / попадает в «Groups» breakdown Analytics (регрессионный тест на баг из §1.3a).
- AC2: смешанная группа (легаси + AccountsCore) — сумма/список = сумме по всем счетам обоих миров (тест).
- AC3: `isFavorite`/`order`/`usesManualAccountOrdering`/`priorityRaw` не теряются после миграции (переносятся все, решение владельца) — тест на конкретный набор групп до/после.
- AC4: идемпотентность миграции — повторный прогон no-op (паттерн Фазы 1).
- AC5: build 0 ошибок; тесты — ноль новых красных vs baseline.
- AC6: `AccountsCoreAdditionBridge.resolveAccountGroup` TODO-комментарий про «Фазу 6» обновлён/удалён (задача выполнена здесь, не в редизайне).

**Риск:** трогает данные пользователя (группы) и три экрана сразу — `/stress-test` + явное «да» владельца обязательны перед мержем, как и для Фазы 1/5.

**Что сделано (2026-07-09):**
- `AccountGroup` расширена 4 аддитивными полями (lightweight-миграция, V6 НЕ трогалась, сборка зелёная): `isFavorite`/`usesManualAccountOrdering`/`priorityRaw:String` + постоянный маркер идемпотентности `legacyFieldsMigratedAt: Date?` (аналог `archivedAt` Ф1). Добавлен computed `priority`. Export/AccountGroupImporter расширены для паритета бэкапа.
- Новый `GroupsMigrator` (`millio/UI/Services/Finances/GroupsMigrator.swift`) — паттерн `LegacyAccountsMigrator`: per-scope UserDefaults-флаг + постоянный маркер `legacyFieldsMigratedAt`. Вызов в `millioApp.runPostStartupRefreshes()` сразу после `LegacyAccountsMigrator`.
- Per-group сумма обоих миров: `AccountsTotalsService.total(for:on:in:)` (сумма по подмножеству счетов) + `FinanceViewModel.calculateGroupTotal` (VM-обёртка, :1061) = legacy + core.
- Dynamics Groups breakdown: группы из core-счетов больше не отбрасываются, core-вклад добавляется к start/end.
- Скрытие Ungrouped учитывает core (`FinanceGroupService.shouldHideGroupInList`); правки редактора синхронизируются на `AccountGroup` (`FinanceGroupService.updateGroup` → `syncCoreGroup`).
- Тесты: `GroupsMigratorTests` (6) + `AccountsTotalsServicePerGroupTests` (2) — зелёные (swift-testing ✔).

**Отклонения от буквального плана (обоснованы, mentor stress-test):**
1. `priorityRaw` = **String** (не Int) — источник `FinanceGroup.priorityRaw` это String, перенос байт-в-байт.
2. Core влит в **VM-обёртку** `FinanceViewModel.calculateGroupTotal` (:1061), а НЕ в `FinanceTotalsService.calculateGroupTotal`: последняя разделяется с агрегатом `calculateTotalsSnapshot`, который добавляет core отдельным лампом (`newCoreTotalProvider`) — правка там задвоила бы core в «Общем балансе». VM-обёртка используется только display-сайтами → AC1/AC2 закрыты, агрегат стабилен (переключение агрегата на single-world — скоуп Ф2, не тронут).
3. `orderedAccounts` НЕ сменён на `AccountGroup` (возвращает `[FinanceAccount]`, унификация типа = рефактор Ф2/Ф6). Список уже дуально-рендерится (`FinanceRows` + `newCoreAccounts`); добавлена только core-осведомлённость скрытия Ungrouped.
4. Редактор «переведён на AccountGroup» = синхронизация полей на `AccountGroup` при сохранении (а не переписывание 669-строчного view — Ф6).

**Self-audit по AC:**
- AC1 ✅ per-group сумма/список/Dynamics-breakdown группы из core-счетов больше не 0/пусто (`AccountsTotalsServicePerGroupTests.totalForSubset…`, breakdown drop-guard + core start/end).
- AC2 ✅ смешанная = legacy + core (независимые вклады складываются; `calculateGroupTotal` = legacy + core).
- AC3 ✅ `GroupsMigratorTests.migratesAllFieldsToCoreGroup` / `reusesExistingCoreGroupByName` — все поля переносятся, дублей нет.
- AC4 ✅ `secondRunIsNoOpAndPreservesUserEdits` (маркер держит, правки юзера не затираются) + `migrateIfNeededShortCircuitsSecondCall`.
- AC5 ✅ build SUCCEEDED; полный `millioTests` — все падения из документированного baseline (18), 0 новых; мои 2 сьюта ✔ (записи в «Failing tests» по GroupsMigratorTests = флаки-запуск клонов симулятора, swift-testing репортит ✔).
- AC6 ✅ TODO/комментарий про «Фазу 6» в `AccountsCoreAdditionBridge.resolveAccountGroup` обновлён.

**⚠️ Перед мержем Ф1.5:** device `/stress-test` + бэкап user-стора + явное «да» владельца (трогает данные групп + 3 экрана).

**Фикс после адверсариального ревью (2026-07-09, коммит поверх e90c1a3):**
- **Дефект 1 (CONFIRMED, medium) — исправлен.** `legacyFieldsMigratedAt` теперь сериализуется в `AccountGroup.export()`/`AccountGroupImporter.import` (как `TimeInterval`, `NSNull()` при nil) — маркер идемпотентности переживает backup/restore, как `archivedAt` у легаси-счетов в Ф1. Без фикса restore на новом устройстве сбрасывал бы маркер в nil → повторный прогон `GroupsMigrator` перезаписывал бы поля `AccountGroup` поверх правок пользователя. Тест-регрессия: `GroupsMigratorTests.restoredGroupWithMarkerIsNotReMigrated` (export → import с новым id, как реальный restore → маркер не nil → повторная миграция no-op, правка юзера не затёрта).
- **Дефект 2 (PLAUSIBLE, low) — задокументирован, не гардится полноценно.** Дубли имён легаси-групп теоретически возможны (UI не проверяет уникальность имени), но не наблюдались в проде (1–2 юзера) — полноценная UI-валидация вне скоупа слияния моделей. Вместо тихого схлопывания: `GroupsMigrator` детектирует дубль явно (`Summary.skippedDuplicateName` + warning-лог с именем группы), первая группа мигрирует поля, остальные не молчат. Задокументировано в докстринге класса как «Известное ограничение». Тест: `GroupsMigratorTests.duplicateLegacyGroupNamesAreLoggedNotSilentlyDropped`.
- Build SUCCEEDED; полный `millioTests` — 0 новых красных vs baseline (все падения, включая `CashflowCategoryHelpContentTests`/`CashflowTransactionEditorViewLayoutTests` zh-Hans locale-leak флаки, входят в `progress/accounts-core-baseline-failures.md`); `GroupsMigratorTests` — 8/8 ✔ (swift-testing).

---

## 5. План отката

- **Ветка:** `feature/legacy-accounts-purge` от свежего `develop`. **Не мержить, не пушить** ночью (условие runbook). Каждая фаза — коммит на чистой границе.
- **Fallback:** develop до сноса (легаси + ядро сосуществуют, рабочее состояние) — точка возврата. Ветка `feature/accounts-core` в истории.
- **Если миграция ломает данные:** миграция обратима — `LegacyAccountConverter.unconvert` удаляет core-двойник и снимает `archivedAt` с легаси (восстанавливает). Флаг «миграция выполнена» в SwiftData сбросить → повторный прогон идемпотентен. На симуляторе — восстановление из `.bak`-стора (механизм `rebuildStorePreservingData`).
- **Схема:** V6 добавляется поверх V5 lightweight-стадией; откат = вернуть `AppSchemaCurrent = V5`, легаси-типы в список. Пока V6 не смержена в develop и не выпущена — откат чистый.
- **Обязательно перед фазой сноса схемы:** бэкап user-стора (`.backup.store`) + `/stress-test` (трогает данные пользователя и протестированную функциональность — правило проекта не отменяется).

---

## 6. Разбивка на фазы (каждая ≈ одна sonnet-сессия)

### 🟢 Фаза 1 — Миграционный слой + переключение чтения (БЕЗ удаления файлов) — САМАЯ БЕЗОПАСНАЯ — [x] РЕАЛИЗОВАН (2026-07-08, ветка `feature/legacy-accounts-purge`, НЕ мержено)
**Что:** новый `LegacyAccountsMigrator` (оркестратор поверх готового `LegacyAccountConverter`); одноразовый прогон при старте user-скоупа под SwiftData-флагом; идемпотентность; перевод чтения total/списка на single-world там, где легаси после миграции пуст. **Ни один файл не удаляется** — легаси остаётся в коде, но данные переехали и легаси-счета скрыты (`archivedAt`). Полностью обратимо (`unconvert`).

**Что сделано:**
- `millio/UI/Services/Finances/LegacyAccountsMigrator.swift` — оркестратор (рядом с `LegacyAccountConversion`, а НЕ в Core: перебирает легаси-@Model, Core остаётся легаси-агностичным). Перебирает активные (`archivedAt == nil`) `Card/Credit/Investment` (широкий fetch + Swift-фильтр, обход ловушки #Predicate §7.2), строит `Plan` → `Input` → `converter.convert`. Группа двойника резолвится из junction `FinanceAccount` по имени.
- Вызов в `millioApp.runPostStartupRefreshes()` синхронно на MainActor, ДО фонового бэкфилла, за per-scope UserDefaults-флагом (паттерн `AccountSnapshotBackfillCoordinator`).
- 11 тестов `LegacyAccountsMigratorTests` — зелёные.

**Отклонения от плана (обоснованы):**
- **SwiftData-флаг → UserDefaults per-scope + `archivedAt`.** Новый @Model = схема V6 = НЕ аддитивная Ф1 (V6 отдана Ф5). Причём цель «переносился при restore» УЖЕ достигнута: гарант идемпотентности — хранимый `archivedAt` (в SwiftData, переносится при restore), а не флаг. Скрытая легаси не перечисляется → повторная миграция после restore невозможна даже при утере реестра/флага. UserDefaults-флаг — лишь короткое замыкание, корректность от него не зависит.
- **Переключение чтения на single-world → отдано Ф2.** Инвариант тотала (AC2) держится двоемирием само: скрытая легаси вносит 0, двойник вносит равный вклад через уже подключённый `newCoreTotalProvider`. Read-switch не нужен для AC Ф1 и это явный скоуп Ф2 (снятие ссылок + снос `AccountTotalPolicy` + тест равенства экранов). Трогать God-VM в Ф1 — лишний риск.
- **Гэп меты вклада/рынка** зафиксирован `TODO(6b/фаза-post)` в шапке мигратора (AC6).

**Self-audit по AC:**
- AC1 ✅ `migratesAllActiveLegacy_totalMatchesLegacyContribution` — все активные → двойник + `isConverted`.
- AC2 ✅ `migratedCoreTotalEqualsSignedSum` (core-тотал = сумма знаковых вкладов; легаси скрыта = 0 → двоемирный тотал неизменен).
- AC3 ✅ `secondRunIsNoOp` + `migrateIfNeeded_flagShortCircuitsSecondRun` (идемпотентность через `archivedAt`, не реестр).
- AC4 ✅ `unconvertRestoresLegacy` (легаси активна, двойник удалён, реестр чист).
- AC5 ✅ build 0 ошибок; полный `millioTests` — 18 baseline-красных, 0 новых (19-й `FinanceDynamicsViewModelTests.testDeleteGroupPreservesArchivedLinkForHistoricalCalculation` — известный флаки, изолированно зелёный).
- AC6 ✅ TODO в коде + отчёт.
- Доп: `emptyStore_isNoOp`, `alreadyArchivedLegacy_notMigrated` (restore-safety), `groupPreservedFromJunction`, `ungroupedJunction_mapsToNilGroup`.

**⚠️ Перед мержем Ф1:** device-level `/stress-test` + бэкап user-стора + явное «да» владельца (правило 7 — стартовая миграция трогает данные пользователя; план не ревьюился).
**Acceptance criteria:**
- AC1: после прогона все не-converted `Card/Credit/Investment` имеют core-двойника; `registry.isConverted` == true для каждого.
- AC2: тотал Accounts/Dashboard/Динамика **не изменился** (opening-balance-двойник = прежний баланс легаси) — тест на сумму до/после.
- AC3: повторный прогон миграции — no-op (идемпотентность, тест).
- AC4: `unconvert` восстанавливает легаси байт-в-байт (тест).
- AC5: build 0 ошибок; полный `millioTests` — ноль НОВЫХ красных относительно baseline; `check-balance-mutations.sh` зелёный.
- AC6: гэп меты вклада/рынка (§3) зафиксирован в коде TODO + в отчёте.

### 🟡 Фаза 2 — Снятие ссылок в total-слое агрегата + снос `AccountTotalPolicy` — [x] РЕАЛИЗОВАН (scoped) (2026-07-10, Александр, ветка `feature/legacy-accounts-purge`, коммит 80ad7cc, НЕ мержено)

**Что реализовано (safe-подмножество, закрывает R1 агрегата):**
- `FinanceTotalsService.calculateTotalsSnapshot` → **только ядро** (`AccountsTotalsService.totalAt`). Снят легаси-цикл суммирования групп + отдельный core-ламп. Дашборд/«Счета» = единственный источник агрегата. Снят `collectCurrenciesFromGroup` (был только для этого цикла).
- `FinanceDynamicsViewModel.updateCurrentBalanceAndDelta`: агрегат заголовка (без скоуп-фильтров: `selectedGroupIDs`/`selectedAccountIDs` пусты, не single-account) получает вклад ядра `totalAt(targetDate)`/`totalAt(startDate)` — **закрывает документированный gap «AccountsCore не в заголовке Динамики»** (unified-totals Фаза 4, была ЗАБЛОКИРОВАНА). Для легаси-фикстур вклад = 0 → легаси-реплей не меняется.
- **`AccountTotalPolicy` СНЕСЁН** (файл удалён). `isActive`/`signedContribution` инлайн-перенесены (поведение-сохраняюще) в `FinanceTotalsService` (per-group display) и `FinanceDynamicsViewModel` (легаси-реплей `calculateBalanceAtDate`). Grep-гейт: `AccountTotalPolicy` не упоминается нигде.
- Тесты: `FinanceTotalsServiceFilterConsistencyTests` переписан на single-world (AC1 равенство Дашборд==Динамика на core-счетах + AC2 скрытая легаси = 0). `AccountTotalPolicyTests` удалён (тестировал снесённый тип). `FinanceViewModelTests`: 3 теста мигрированы (агрегат `state.totalAmount` теперь по ядру) — 74/74 ✔.

**Отклонения от буквального плана (обоснованы, mentor stress-test — доказаны кодом/тестами):**
1. **`mergingNewCoreSeries` НЕ снесён (AC4b перенесён в Ф2b).** Он **несущий**: аггрегированный график строит легаси-скелет дат (`buildTimeSeriesData`) и стыкует к нему ядро поточечно. Пост-миграции легаси даёт скелет (архивные счета, time-aware), ядро — значения; удаление merge оставляет график без core-истории ИЛИ пустым (buildTimeSeriesData на пустых легаси → `[]`, merge на `[]` → `[]`). Плюс это ломает протестированную регрессию `testAggregatedChartUsesHistoricalAccountsWhileHeaderAndBreakdownStayVisible` (архивная история графика). Снос = регрессия истории + слом protected-тестов → требует порт per-account Dynamics на ядро («1b») + device stress-test.
2. **Унификация `FinanceRows`/`newCoreAccounts`/`orderedAccounts(for:)` → `[Account]` НЕ сделана (Ф4).** Легаси-ветка списка **инертна пост-миграции** (доказано: `cardByID`=`cardByIDAvail` фильтрует `archivedAt != nil`, `getAccountInfo`→nil→`orderedAccounts` пусто). Смена типа `orderedAccounts`→`[Account]` орфанит `FinanceAccountRow`/`getAccountInfo`/`customIconInfo`, которые **ещё потребляются** `FinanceDynamicsView`/`FinanceQuickEditAccountView` (снос — Ф4). Не трогает ни один AC (все AC про тотал).
3. **`FinanceCreateViews`/`FinanceEditorWrappers`/`InlineCreateForms` НЕ снесены (Ф3/Ф4).** Grep **опроверг** «мёртвый код»: `FinanceCreate*View` референсятся `FinancesSheets.swift`, `Inline*CreateForm` — `FinanceAddAccountView.swift`. Это легаси-**писатели** (создают Card/Credit/Investment) = порядок §2 шаг 3 (Ф3/Ф4), не total/read-слой Ф2.
4. **`FinanceViewModel.calculateGroupTotal` оставлен bolt-on (legacy + core).** Это DISPLAY-only (карточка группы/строки), **не задваивается** в агрегат (агрегат теперь core-only ламп, per-group туда не суммируется). Пост-миграции = core; при сносе легаси (Ф4/5) легаси-терм схлопывается сам. Перевод на core-only ломает ~11 легаси per-group тестов `FinanceViewModelTests` без выигрыша по AC.

**Self-audit по AC:**
- AC1 ✅ `FinanceTotalsServiceFilterConsistencyTests.aggregateTotalMatchesAcrossDashboardAndDynamics` — Дашборд == Динамика == core-сумма (17 500) на наборе core-счетов.
- AC2 ✅ `hiddenLegacyContributesNothing` — core + скрытая (archived) легаси-карта 99 999 → тотал = core-сумме (9 000), легаси вносит 0.
- AC3 ✅ build 0 ошибок; затронутые сьюты зелёные (FilterConsistency 2/2, FinanceViewModelTests 74/74, FinanceDynamicsViewModelTests 90/0, AccountsTotals/Migrators ✔). Полный `millioTests`: остаточные падения = baseline (recurring/notification/l10n/archive/lifecycle) + l10n zh-Hans флаки + swift-testing wrapper-артефакт (пустой «Failing tests» при ✔ сьютах). 0 новых красных. Удалён: `AccountTotalPolicyTests` (снесённый тип). Переписан: `FinanceTotalsServiceFilterConsistencyTests` (обсолетная легаси-агрегатная посылка).
- AC4a ✅ `AccountTotalPolicy` grep-гейт чист. AC4b ⏳ `mergingNewCoreSeries` → Ф2b (обоснование выше).

**⚠️ Перед мержем Ф2:** device `/stress-test` + бэкап user-стора + явное «да» владельца (трогает агрегат тотала на трёх экранах + заголовок Динамики).

**Известное ограничение MVP (не чинить, зафиксировано):** дельта «Динамики» через границу миграции искажена — стартовый баланс периода ДО даты миграции считается по легаси-реплею (`coreStart=0`, т.к. у core-двойника ещё нет истории на ту дату), а конечный баланс уже включает core opening-balance с даты миграции; на периодах, пересекающих границу миграции, это даёт разовый скачок дельты. Приемлемо при 1–2 юзерах и согласованной потере истории (см. §3); чинится только полным портом Dynamics на ядро («1b», Ф2b).

**Фикс адверсариального ревью (2026-07-10, коммит после 80ad7cc):** CONFIRMED-дефект — transient «Общий баланс ≈ 0» на первом запуске после апдейта. Причина: `LegacyAccountsMigrator`/`GroupsMigrator` вызывались в `runPostStartupRefreshes()` (`millioApp.swift`) ПОСЛЕ того, как `useCase.initialize()` выставлял `lifecycle = .ready` и монтировал RootTabView/FinanceViewModel — у легаси-юзера core-двойников в этот момент ещё нет, агрегат (теперь core-only, Ф2) считался 0, и ни мигратор, ни конвертер не публикуют `FinanceEvent` → авто-пересчёта нет, transient держится до случайного триггера или рестарта. **Выбран вариант (а)** (полное устранение окна, не смягчение): миграция вынесена в новый `runLegacyAccountsMigrationIfNeeded()` и вызывается ВНУТРИ `onScopeResolved` в `synchronizeDataScope`, ДО `useCase.initialize()` — на финальном (пост-swap) контейнере, синхронно на MainActor, до монтирования RootTabView. Миграция и так синхронная/быстрая (1–2 счёта, без сети) — перенос раньше `.ready` не меняет её стоимость, только устраняет нулевой кадр целиком (вариант (б) — событие — оставлял бы короткое мигание, поэтому не выбран). Тест регрессии: `LegacyMigrationOrderingTests` (2 теста) — секция 1 документирует механизм бага (старый порядок: расчёт до миграции → 0, без авто-хила), секция 2 доказывает фикс (новый порядок: миграция до расчёта → корректно с первого раза). `millioApp.swift` недоступен юнит-тестам напрямую (SwiftUI `App`) — порядок вызовов воспроизведён напрямую тем же приёмом, что `FinanceViewModelTests.testCalculateTotalAmountDoesNotForceRefreshRates`.

**Побочная находка при гейте (не мой скоуп, флагирую честно):** `GroupsMigratorTests` (Ф1.5, файл не тронут в Ф2/Ф2b) при прогоне через `xcresulttool` (ground truth, не текстовый summary xcodebuild) стабильно падал с «Test crashed with signal trap» — `makeStack()` возвращал `container.mainContext`, не удерживая сам `ModelContainer` (нет `retainedContainers`), из-за чего in-memory store мог быть освобождён сразу после return (use-after-free). Тот же класс бага я поймал и исправил в СВОЁМ новом `LegacyMigrationOrderingTests` при первой попытке. Важно: прежний self-audit Ф1.5 «8/8 ✔» и мой собственный ранний self-audit Ф2 «0 новых красных, wrapper-артефакт» были ОШИБОЧНЫ в части `GroupsMigratorTests` — консольный текстовый summary xcodebuild («✔ Suite … passed») ненадёжен и может показывать ложный ✔ при реальном крэше; впредь для крэш-подозрительных сьютов проверять через `xcrun xcresulttool get test-results summary --path <bundle>` (JSON `passedTests/failedTests`), не через grep по тексту. **✅ Исправлено другим агентом (коммит `3a81230`, между Ф2-ревью и Ф2b)** — тем же паттерном `retainedContainers`; подтверждено при гейте Ф2b: `GroupsMigratorTests` больше не в списке падений, полный сьют (1710 passed / 15 failed) — все 15 совпадают с документированным baseline.

### 🟢 Фаза 2b — Снос dual-path `mergingNewCoreSeries`, единый core-ряд истории графика — [x] РЕАЛИЗОВАН (2026-07-10, Александр, ветка `feature/legacy-accounts-purge`, коммит `49fe371`, НЕ мержено)

**Что реализовано:** `ChartDataPoint.mergingNewCoreSeries` (`FinanceDynamicsViewModel.swift`, был :145) — статическая функция поточечного слияния легаси-скелета графика с ОТДЕЛЬНО построенным дневным рядом ядра (`AccountsTotalsService.seriesBetween`) через forward-fill приближение (ближайший ПРЕДЫДУЩИЙ день) — **снесена**. Замена — приватный `addingCoreContribution(_:currency:)`: для каждой уже построенной точки легаси-скелета (`buildTimeSeriesData`, НЕ тронут) делает точный запрос `accountsTotalsService.totalAt(point.date, in: currency)` НА ТУ ЖЕ дату и складывает. Убирает промежуточный массив + эвристику совпадения дат целиком (dual-path → один проход).

**Отклонение от буквального «один ряд seriesBetween» (обосновано, mentor stress-test — доказано кодом):** Литеральная замена на ЧИСТО core-only `seriesBetween` как единственный источник дала бы **0 для всех дат ДО миграции** — `LegacyAccountConverter.convert` создаёт opening-событие core-двойника датой МИГРАЦИИ (`date: Date = Date()`), а не оригинальной датой легаси-счёта (§3: «MVP: переносит opening-balance… без реплея истории»). Это стёрло бы РЕАЛЬНУЮ легаси-историю (доказано time-aware проверкой в `calculateBalanceAtDate`: архивный счёт отдаёт настоящий баланс для дат `<= archivedAt`) для ВСЕХ уже мигрированных пользователей — регрессия ХУЖЕ задокументированного MVP-ограничения (то — про искажение ДЕЛЬТЫ на границе, не про полную потерю ГРАФИКА). Сохранён легаси-скелет как каркас дат/доисторических значений; `seriesBetween` (дневной проход по всему периоду) заменён на точечный `totalAt` по датам уже построенного (обычно редкого, event-driven) скелета — не хуже по производительности, точнее (нет forward-fill приближения).

**Protected-тесты:** ни один из 45 тестов `FinanceDynamicsViewModelTests` не переписан — их схема (`Schema([Card, Credit, Investment, FinanceGroup, FinanceAccount, CashflowTransaction, HistoricalRate])`) не регистрирует `Account`/`AccountEvent`, поэтому `hasAnyAccounts()`/`totalAt` внутри `AccountsTotalsService` рано выходят (COUNT=0, `try?`-safe даже без типа в схеме) и ядро ВСЕГДА добавляло 0 — идентично старому dual-path (`seriesBetween` тоже рано выходил на пустом ядре). Итоговое поведение для этих тестов НЕ изменилось. `ChartDataPointMergeTests.swift` (тестировал снесённую функцию напрямую) — **удалён**; заменён `FinanceDynamicsCoreContributionTests` (2 теста, тестируют КОНТРАКТ через публичный `updateChartDataAsync()`, не приватную реализацию): (1) мигрированный легаси-счёт — доисторическая точка = легаси-значение (100 000, не искажена), текущая точка = вклад ядра (100 000, НЕ обнулилась вместе с легаси); (2) без core-счетов — график не меняется (документирует известный отдельный пробел «1b», не регрессия).

**Self-audit по AC:**
- AC1 (спроектирована single-world замена, задокументированное MVP-ограничение не усугублено) ✅ — `addingCoreContribution` сохраняет доисторическую легаси-точность и добавляет ядро точно на дату; тест `migratedAccount_preservesLegacyHistoryAndCarriesCoreValueForward`.
- AC2 (dual-path снесён, protected-тесты обработаны — переписаны/сохранены с обоснованием) ✅ — `mergingNewCoreSeries` удалена, `ChartDataPointMergeTests` удалён (тестировал снесённое), 45 protected Dynamics-тестов НЕ ТРОНУТЫ (доказано: поведение идентично для схемы без `Account`), 2 новых теста на контракт.
- AC3 (build 0 ошибок, Dynamics-сьют полностью + скан полного сьюта — 0 новых красных, ТОЛЬКО через xcresulttool) ✅ — build+build-for-testing чисто; `FinanceDynamicsCoreContributionTests` 2/2; `FinanceDynamicsViewModelTests` 45/45; полный `millioTests` — 15 failed, все совпадают с baseline (см. `progress/accounts-core-baseline-failures.md`), `GroupsMigratorTests` не в списке (фикс 3a81230 держит).
- AC4 (grep-гейт: `mergingNewCoreSeries` нигде) ✅ — только исторические комментарии («снесён»/«замена»), ни одного вызова.

**⚠️ Перед мержем Ф2b:** трогает график «Динамики» (визуальные данные пользователя) — `/stress-test` + явное «да» владельца, как и для Ф1/Ф1.5/Ф2.

### 🟠 Фаза 2c — Остаток single-world read-слоя (escalated, требует «1b» + device stress-test)
Переименовано из «Фаза 2b» после того, как пункт (а) реализован отдельно как Фаза 2b (см. выше, коммит `49fe371`) — осталось два пункта. Вынесено из Ф2 как доказанно-рискованное (см. отклонения Ф2). Пункты: (б) унификация `FinanceRows`/`orderedAccounts(for:)`→`[Account]` со сносом `FinanceAccountRow`/`getAccountInfo`-каскада (координировать с Ф4); (в) перевод `VM.calculateGroupTotal` на core-only (после сноса легаси Ф4/5 — само схлопнётся). Port per-account chart-modes (byAccounts/singleAccount) на ядро и скоуп-заголовка («1b») — тоже сюда. AC: список счетов single-world, `VM.calculateGroupTotal` без легаси-терма, per-account/byAccounts Dynamics на ядре, device-проверка.

### 🟡 Фаза 3 — Миграция живых писателей: `StockBulkImportServices` + `ScreenshotDataSeeder` — [x] РЕАЛИЗОВАН (scoped) (2026-07-10, Александр, ветка `feature/legacy-accounts-purge`, коммит `52fff71`, НЕ мержено)

**Что реализовано:**
- `StockBulkImportServices.swift` (`StockBulkImportPersistenceService.persist`) — единственная точка записи теперь `AccountsCoreService.createAccount(kind: .marketInvestment, marketMeta:)` + `service.buy(...)`; легаси `Investment`/`FinanceAccount` больше не создаются. Дедуп по bare-символу (не `storedSymbol` с префиксом биржи — префикс сломал бы `AccountMarketPriceService.refreshTodayPrices()`, которая шлёт `meta.symbol` в `fetchQuotes` буквально); группа резолвится тем же `AccountsCoreAdditionBridge.resolveAccountGroup`, что использует живой одиночный add-flow. `StockBulkImportMatcher.loadLocalCatalog()` расширен дуальным чтением легаси `Investment` + core `Account` (иначе автодополнение «уже добавлено» замолчало бы для новых импортов).
- 3 теста `StockBulkImportTests` переписаны на core-Account сборку (мердж с уже существующим core-счётом — посылка «позиция из обычного редактора это легаси Investment» устарела: обычный редактор (`FinanceAddAccountView.createAssetAccountOnNewCore`) уже давно на `AccountsCoreService`, найдено при разборе).
- Grep-гейт (конструкторы `= Investment(`/`Card(`/`Credit(`/`FinanceAccount(` вне тестов/конвертера): единственные оставшиеся — `CardEditorView`/`CreditViewModel`/`InvestmentViewModel`/`*FeatureRegistration`/`FinanceAccountService` — это ПРАВКА существующих легаси-записей (`updateCard`/`updateInvestment`, старый путь редактирования, docstring `AccountsCoreAdditionBridge`), их снос — явный скоуп Ф4 («снос редакторов/VM/менеджеров»), не Ф3.

**Отклонения от буквального плана (обоснованы, mentor stress-test — доказаны кодом):**
1. **`ScreenshotDataSeeder` НЕ мигрирован — доказанный блокер, не лень.** `Cashback.cardIDs` резолвится СТРОГО против легаси `Card` (`CashbackViewModel.swift:594-606` фильтрует `cardIDs` через `FetchDescriptor<Card>`; `CashbackFeatureRegistration.swift:79` тоже). Cashback НИГДЕ не упомянут в плане 6b как мигрируемая фича — нет моста к `AccountsCore`. Перевод `seedCards()` на `Account` молча обнулил бы cashback-скриншоты (проценты привязаны к `Card.uniqueID`, ядро не даёт такого ID легаси-типу) — это РЕГРЕССИЯ маркетинговых скриншотов (Nova), не абстракция ради гейта. Файл гарантированно безопасен для реальных юзеров: вызов только под `runtimeEnvironment.isScreenshotMode` (`millioApp.swift:174`, fastlane-only, никогда true в проде). Требует ЛИБО отдельной миграции Cashback на AccountsCore (новый скоуп, не в этом плане), ЛИБО принятия что `Card.swift` переживёт остальные легаси-модели в Ф5 как spec-исключение. Эскалировано во владельцу — решение нужно ДО Ф5 (схема V6 не сможет удалить `Card.swift`, пока Cashback на неё завязан).
2. **`FinanceCreateViews.swift` (`FinanceCreateCardView`/`CreditView`/`InvestmentView`) — подтверждена ПОЛНАЯ мёртвость (не просто «referenced»).** Ф2 нашёл, что `FinancesSheets.swift` их РЕФЕРЕНСИТ (компилируется), но триггер (`viewModel.handle(.showCreateCardSheet)`/`.showCreateCreditSheet`/`.showCreateInvestmentSheet`) НИГДЕ не вызывается во всей кодбазе (grep: только объявление action + reducer-case в `FinanceViewModel.swift`, ни одного вызова из View/кнопки) — `showCreateCardSheet`/etc. навсегда `false`. Живой «добавить счёт» флоу — только `FinanceAddAccountView` (через `showAddAccountSheet`). Полный вычищенный снос (сами Views + ветки `FinancesSheets.swift` + state/actions `FinanceViewModel.swift`) — Ф4 («снос редакторов»), не Ф3 (Ф3 — только писатели, эти вьюхи мертвы, не пишут).
3. **`InlineCreateForms.swift` (`InlineCardCreateForm`/`InlineCreditCreateForm`/`InlineInvestmentCreateForm`) — НЕ являются писателями легаси, изменений не требуют.** Живые (используются `FinanceAddAccountView` как поля формы), НО их `Card(...)`/`Credit(...)`/`Investment(...)` — транзиентные `@State`-DTO для биндингов (grep подтвердил: НИ ОДНОГО `modelContext.insert(...)` во всём файле) — реальная запись идёт через `AccountsCoreAdditionBridge`+`AccountsCoreService` в `FinanceAddAccountView`, DTO выбрасывается. Формально нарушают буквальный grep-гейт AC1 (конструктор есть), но НЕ нарушают инвариант, ради которого гейт существует (запись). Жёсткая зависимость: пока эти DTO типизированы как `Card`/`Credit`/`Investment`, Ф5 не сможет удалить сами `@Model`-файлы без ОДНОВРЕМЕННОГО рефакторинга этого файла (замена на простые struct) — компромисс между «сделать сейчас чисто» и «не трогать живую форму добавления счёта без device-стресс-теста» решён в пользу отложить: рефакторинг DTO-типов запланирован как явная под-задача Ф4 (риск ниже, чем сейчас — Ф4 и так трогает эти типы при сносе моделей).

**Self-audit по AC:**
- AC1 (ни один путь не создаёт легаси) — ✅ для ЖИВЫХ путей (StockBulkImport мигрирован, InlineCreateForms не пишет, create-views мертвы); ⏳ ScreenshotDataSeeder — доказанный блокер (Cashback), не входит в «живые пути» (screenshot-only), эскалирован; редакторы/VM (Ф4-скоуп) остаются до Ф4.
- AC2 (build 0 ошибок; 0 новых красных, только xcresulttool) — ✅ build SUCCEEDED (`build` + `build-for-testing`); `StockBulkImportTests` 24/24 (xcresulttool json); полный `millioTests` — 22 failed = 16 задокументированных baseline + 1 LanguageManager-flaky + **5 нового flaky-класса** (UserDefaults.standard rate-cache leak в `CurrencyRateServiceTests`/`FinanceViewModelTests`, задокументирован в `progress/accounts-core-baseline-failures.md`, доказано изоляцией: воспроизводится при прогоне ТОЛЬКО этого класса на файлах, не тронутых Ф3) — ноль падений от диффа Ф3.
- AC3 (тесты на переведённые пути) — ✅ 3 теста `StockBulkImportTests` (создание core-счёта с buy-событием; дедуп без mergeDuplicates; мердж с уже существующим core-счётом).

**⚠️ Перед мержем Ф3:** трогает единственный писатель bulk-импорта акций (реальные пользовательские данные при следующем импорте) — `/stress-test` + явное «да» владельца, как и предыдущие фазы.

**Решение нужно от владельца (эскалация, не блокирует Ф4/Ф5 полностью, но блокирует ОДИН файл в Ф5):** судьба `ScreenshotDataSeeder`/Cashback-Card-связки — (а) отдельно мигрировать Cashback на AccountsCore (новый скоуп), (б) оставить `Card.swift` как spec-исключение дольше остальных легаси-моделей, (в) принять деградацию cashback-скриншотов. Рекомендация ментора: (б) — дешевле всего, `Card.swift` и так последний кандидат на снос (Cashback — единственный оставшийся потребитель после Ф4).

### 🟠 Фаза 4 — Упрощение мостов + снос редакторов/VM/менеджеров — [x] РЕАЛИЗОВАН (scoped) (2026-07-10, Александр, ветка `feature/legacy-accounts-purge`, коммит `5221736`, НЕ мержено)

**Что реализовано (safe-подмножество — снос доказанно-мёртвой легаси-редактор-поверхности):**
- Снесены 6 файлов + осиротевший тест: `CardEditorView.swift`, `CreditEditorView.swift`, `InvestmentEditorView.swift` (легаси-редакторы), `Editors/FinanceEditViews.swift` (презентеры `FinanceEditCardView`/`CreditView`/`InvestmentView`), `Editors/FinanceEditorWrappers.swift` (3 wrapper-структуры — референсились нигде), `Editors/FinanceCreateViews.swift` (Ф3-доказанно мёртвые), `millioTests/.../InvestmentEditorLayoutPolicyTests.swift` (единственный потребитель `InvestmentEditorLayoutPolicy`).
- `FinanceViewModel.swift`: убрана мёртвая обвязка — state (`showCreate{Card,Credit,Investment}Sheet`, `showEdit{Card,Credit,Investment}Sheet`, `editing{Card,Credit,Investment}ID`), actions (`showCreate*`/`hideCreate*`, `editAccount`, `hideEdit*`), reducer-кейсы, метод `editAccount()`. **Доказательство мёртвости:** ни `.editAccount`, ни `.showCreateCardSheet`/etc. НИГДЕ не диспатчатся (`rg "handle(.…)"` пуст) — тот же паттерн мёртвого action-case, что Ф3.
- `FinancesSheets.swift`: убраны 6 мёртвых sheet-блоков (3 create + 3 edit), референсивших снесённые вью.
- `AccountsCoreDepositCashflowBridge` — верифицирован чистым (резолвит только `AccountEvent`/`Account`/`CashflowTransaction`, `Investment` не трогает), правок не требует (AC моста).
- Тест-гард: `AllPresetsOnNewCoreTests.editingLegacyReturnsNilForEveryResolver` (all 11 options) — в edit-режиме все 4 резолвера моста → nil. Инвариант «create-режим → ровно один core-kind» уже гардит `eachPresetResolvesToExactlyOneNewCoreKind`.

**Отклонения от буквального плана §4 (обоснованы, mentor stress-test — доказаны кодом/грепом, file:line):**
1. **Гуарды `isEditingLegacy` НЕ удалены — посылка §4 «мёртвые после Ф1» ОПРОВЕРГНУТА.** Греп-трасса: легаси-EDIT-путь ЖИВ — `FinanceDynamicsView:778/2498` ставит `showFullProductEditSheet=true` → `fullProductEditSheetContent` (:462-468) строит `FinanceAddAccountView(editingCard: resolvedCard(for:), …)` → в `addAccount()` (:1386) при `editingCard != nil` резолверы вернули nil из-за гуарда → легаси `updateCard`/`updateCredit`/`updateInvestment` (:1577/1594/1689 через `updateCardAndGroup`/etc.). Снос гуарда воскресил бы баг Ф6a (правка легаси → дубль core-Account). Плюс гуард дефенсивен в окне раскатки миграции. Легаси-CREATE-путь при этом мёртв (create всегда даёт core-kind). Гуарды → Ф5 вместе со сносом легаси-путей `FinanceAddAccountView` (живой экран → device stress-test).
2. **`AccountsCoreCashflowBridge` НЕ упрощён — легаси-ветки НЕСУЩИЕ до Ф5.** Проверено кодом: миграция НЕ перепривязывает `CashflowTransaction.cardID` (`LegacyAccountsMigrator`/`Converter` транзакции не трогают), историч. операции остаются на легаси-`Card.uniqueID`, легаси `Card` физически жив (только `archivedAt`) до Ф5. При ре-синке такой операции `resolveNewCoreAccount(легаси-ID)` ОБЯЗАН вернуть nil, чтобы НЕ создать дубль core-события (легаси-баланс уже влит как opening core-двойника). «Чистый new↔new» = скоуп Ф5.
3. **`CardCatalog`/`CardManager`/`FinanceAccountService`/`FinanceInvestmentOrderService` НЕ снесены — греп опроверг мёртвость (живут в God-VM/Cashback).** `CardCatalog` — пикер Cashflow (`CashflowViewModel:416 state.allCards`, `+Categories:399/403`, `PersistenceService:430`); `CardManager` — пикер Cashback (`CashbackViewModel:112/626 state.availableCards`); `FinanceAccountService` — 11 живых сайтов FinanceViewModel (archive/restore/`physicallyDeleteLegacyAccount` архивных легаси-счетов, живо до Ф5); `FinanceInvestmentOrderService.executeInvestmentOrder` — резолвит легаси `Investment(isMarketPriced)`, диспатч из живого `FinanceDynamicsView:1319`. Снос каждого = миграция фичи на ядро (Cashflow-пикер / Cashback / управление легаси / инвест-ордера), трогает user-data + живые экраны (rule 7) → Ф5/выделенные фазы.
4. **VM `CardViewModel`/`CreditViewModel`/`InvestmentViewModel` + рефактор `InlineCreateForms` DTO → Ф5.** VM живы через легаси-EDIT-путь (откл. 1) + транзиентные DTO `InlineCreateForms` (Ф3-находка). Снос требует рефактора живого 1793-строчного экрана добавления счёта → device stress-test.

**Cashback-узел (AC4) — эскалация Ф3 разобрана, остаточный блокер Ф5 задокументирован:**
- Это НЕ единичная точка резолва, а СКВОЗНАЯ зависимость фичи Cashback от легаси `Card`: `CardManager.getAllCards()` фильтрует `archivedAt` → пост-Ф1 `state.availableCards` ПУСТ (пикер кэшбэка пуст, связи cashback→card осиротели уже сейчас); `cleanInvalidCardIDs` строит `[String: Card]` и удаляет невалидные cardID; `CashbackFeatureRegistration:79-82` валидирует бэкап через `FetchDescriptor<Card>`; `CashbackView:1697` фильтрует. Cashback НИГДЕ в плане 6b не значится как мигрируемая фича.
- **Ключ к миграции есть:** `LegacyConversionRegistry.coreAccountID(forLegacyUniqueID:) -> UUID?` даёт legacy→core UUID мэппинг для одноразовой перезаписи `Cashback.cardIDs` при будущей миграции.
- **Решение (mentor):** безопасный полный фикс = ВЫДЕЛЕННАЯ фаза миграции Cashback→AccountsCore (пикер на `Account` kind=card, резолв cardIDs на core, backup-import, `CashbackView`-фильтры, метрики) — трогает user-data, требует device stress-test + «да» владельца. Минимальный «dual-резолв» в ночь НЕ shipнут: либо не разблокирует Ф5 (тип `Card` всё равно референсится), либо рискует стереть cashback-связи; активная перезапись сейчас не срабатывает (`cleanInvalidCardIDs` гейтится, на пустом `availableCards` НЕ триггерит — проверено). **Остаточный блокер Ф5:** `Card.swift` нельзя удалить, пока Cashback на него завязан. Рекомендация (совпадает с Ф3): **(б) — `Card.swift` живёт как spec-исключение дольше остальных легаси-моделей**, до выделенной миграции Cashback. Требуется решение владельца.

**Self-audit по AC:**
- AC1 ✅ build 0 ошибок (только пред-существующие warnings `DataRepository`/`LegacyConversionRegistry.shared`); полный `millioTests` — 1709 passed / 26 failed (xcresulttool), ВСЕ 26 = документированный baseline/flaky (12 baseline + 6 LanguageManager-flaky + 5 rate-cache-flaky + 2 `FinanceDynamicsCoreContributionTests` [Ф2b, зелёные в изоляции] + 1 `GroupsMigratorTests` [Ф1.5 order-flaky, run1 зелёный/run2 красный на одном коммите — доказано]) — 0 новых красных от диффа Ф4.
- AC2 ⏳→документировано: мосты НЕ упрощены — легаси-ветки доказанно НЕ мёртвые (откл. 1-2); `DepositCashflowBridge` верифицирован чистым. «Упрощение мостов» = Ф5.
- AC3 ✅ grep-гейт: снесённые типы (`CardEditorView`/`CreditEditorView`/`InvestmentEditorView`/`FinanceEditCardView`/`FinanceCreateCardView`/wrapper-структуры/`InvestmentEditorLayoutPolicy`/`showCreate*`/`showEdit*`/`editing*ID`/`.editAccount`) — ни одного упоминания.
- AC4 ✅ Cashback-узел разобран; остаточный блокер Ф5 (`Card.swift`) явно задокументирован + рекомендация (б) + ключ миграции (`LegacyConversionRegistry`).

**⚠️ Перед мержем Ф4:** снос дохлого UI-кода без изменения логики/данных — риск низкий; device-проверка (редакторы недостижимы, add/edit-flow работает) желательна на общем гейте ветки.

### 🟢 Фаза 5a — Cashflow: пикер cash-подобных инвестиций → ядро, ремап истории, снос мёртвых инвест-ордеров — [x] РЕАЛИЗОВАН (2026-07-10, Александр, ветка `feature/legacy-accounts-purge`, коммит `7d5e906`, НЕ мержено)

**Что реализовано:**
- `LegacyAccountConversion.plan(for: Investment)` — cash-подобная Investment (`isCashflowAccount`, пресет «Счёт») теперь мигрирует в `.bankAccount` (было `.manualAsset` для ВСЕХ Investment без разбора). `.bankAccount` уже входит в `cashLikeKinds` (`CashflowViewModel+AccountsCore.swift`) — счёт автоматически появляется в пикере Cashflow как `.card`-опция после архивации легаси. Рыночные/вклад/недвижимость/бизнес/долг — без изменений (`.manualAsset`, документированная деградация §3 не усугублена).
- `LegacyAccountsMigrator.remapCashflowHistory()` (новый шаг в существующем файле, не новый класс) — переписывает `cardID`/`toCardID`/`investmentID` СУЩЕСТВУЮЩИХ `CashflowTransaction` с легаси-`uniqueID` на `Account.id` core-двойника через `LegacyConversionRegistry`, ТОЛЬКО если резолвленный двойник — денежный kind (`cash`/`debitCard`/`bankAccount`). Без этого `AccountsCoreCashflowBridge.resolveNewCoreAccount` (сверяет `cardID` буквально с `Account.id`, разные пространства UUID) теряет атрибуцию счёта у дотранзакционной истории после сноса легаси-моделей (Ф5c). Вызывается на КАЖДЫЙ прогон `migrateAll()` (ловит счета, сконвертированные в Ф1/Ф3 ДО появления этого шага). Идемпотентно по построению.
- **`инвест-ордера → ядро` переосмыслено как снос, не порт** (mentor stress-test, доказано грепом до правки): `FinanceInvestmentOrderService.executeInvestmentOrder`/`tradeSheet`/`marketInvestmentDetailsView` в `FinanceDynamicsView` достижимы ТОЛЬКО через `marketInvestment` (`FinanceViewModel.getMarketInvestment` → `investmentByIDAvail`, archived-фильтр) — после Ф1-миграции ВСЕ легаси `Investment` архивированы → ветка НАВСЕГДА недостижима (тот же класс доказательства, что Ф2/Ф4 dead-editor). Порт 563-строчного hand-rolled trading-движка (buy/sell, avg-price, settlement-нога) на живые деньги реальных пользователей — риск СУЩЕСТВЕННО выше, чем удаление доказанно мёртвого кода. Снесены: `FinanceInvestmentOrderService.swift` целиком, ~880 строк market-trade UI в `FinanceDynamicsView` (marketInvestmentDetailsView/tradeSheet/statCell/actionButton/tradeSettlementAccountMenu/syncMarketDraft/etc.), `FinanceTradeCelebrationOverlay.swift`+`FinanceTradeCelebrationHapticsPlan.swift` (overlay триггерился только из снесённого кода), `InvestmentOrderSide`/`InvestmentOrderFunding`/`FinanceTradeCelebration`/`eligibleSettlementCards`/`eligibleSettlementAccounts` + reducer-кейсы в `FinanceViewModel`. 6 живых-toolbar/layout call sites, гейтившихся на `marketInvestment == nil` (всегда true post-миграции), — заменены на доказанные константы (`true`/`false`/`!isSingleAccountMode`), НЕ восстановлен фиктивный computed var. `currentDynamicsGroup`/`singleGroupActionBar`/`confirmDeleteCurrentGroup` (используют `FinanceGroup`, НЕ market) — НЕ тронуты, отдельный live-путь.
- Тесты: снесены `FinanceInvestmentOrderServiceTests.swift`, `FinanceTradeCelebrationHapticsPlanTests.swift`, 5 тестов `FinanceViewModelTests` (executeInvestmentOrder/eligibleSettlementAccounts), 1 тест `FinanceLifecycleIntegrationTests` (deletingMarketBuyRevertsSettlementAndPositionTogether), `executeInvestmentOrder`-хелпер `FinanceLifecycleHarness`.

**Гейт:** `xcodebuild build` SUCCEEDED (derivedData `/tmp/millio-f5-build`), 0 ошибок. Полный тестовый прогон отложен до финального гейта Ф5c (по инструкции — быстрый build-чек после 5a/5b, полный сьют только в конце).

### 🟢 Фаза 5b — Cashback: ремап cardIDs + фикс backup-import (scoped) — [x] РЕАЛИЗОВАН (2026-07-10, Александр, ветка `feature/legacy-accounts-purge`, коммит `c7ef57a`, НЕ мержено)

**Что реализовано:**
- `LegacyAccountsMigrator.remapCashbackCardIDs()` — переписывает `Cashback.cardIDs` тем же `remappedCashLikeAccountID` (переиспользован из Ф5a, не задублирован).
- `CashbackFeatureRegistration.CashbackImporter` — валидация `cardIDs` на restore расширена: принимает легаси `Card.cardUniqueID` ИЛИ core `Account.id.uuidString` (была строго `FetchDescriptor<Card>` — **CONFIRMED-баг**: restore бэкапа, снятого ПОСЛЕ ремапа, ВСЕГДА падал бы `AppError.backupCorrupted`, т.к. ни один ремапленный core-UUID не проходил старую проверку).

**Эскалация (mentor stress-test, НЕ реализовано в этом коммите):** пикер Cashback (`CardManager.getAllCards()` → `[Card]`, `CashbackViewModel.state.availableCards: [Card]`, `CashbackView.swift` ~2367 строк, `CashbackCardPresentation.swift`) остаётся легаси-only — новые core cash-подобные счета НЕ появляются как цель кэшбэка. Полный порт требует новой абстракции уровня `CashflowSelectableAccount` (Cashflow УЖЕ имел этот слой готовым до этого плана — Cashback его никогда не имел) поверх `Card`+`Account` с разной формой meta (легаси `creditLimit`/`cardType` не существует на core-стороне: `AccountKind` не различает дебет/кредит). Это НОВАЯ фича (абстракция + UI), не ponytail-порт — отложено. Подтверждено безопасно: `cleanInvalidCardIDs` не триггерит на пустом `availableCards` (гейт `isFirstLoad`/`cardsWereDeleted` требует непустого списка) — ремап не создаёт риска потери данных сейчас; но `CardManager`/`CardCatalog`/`CashbackView`/`CashbackViewModel`/`CardViewModel`/`CreditViewModel`/`InvestmentViewModel` в любом случае должны быть решены в Ф5c (компиляция форсирует правку при сносе `Card.swift`).

**Гейт:** `xcodebuild build` SUCCEEDED, 0 ошибок.

### 🔴 Фаза 5c — Снос регистрации, бэкап-импортёров, схема V6, @Model-файлы — ЗАБЛОКИРОВАНА (не начата, 2026-07-10)

**Почему остановлено (mentor call, не отказ от задачи):** свежий grep после Ф5a/5b показал **76 файлов** ещё ссылаются на `Card`/`Credit`/`Investment`/`FinanceAccount`/`FinanceGroup` в компилируемом коде — существенно больше, чем «остаток одного захода», доказанный по факту исполнения (не предположение):
- **`CashbackView.swift` (2367 строк) + `CashbackViewModel.swift` (1141 строка)** — вся фича кэшбэка типизирована на `Card` (пикер, `cardIDs`-резолв, presentation-слой); порт требует новой абстракции (см. Ф5b эскалацию), это M-размера под-проект сам по себе.
- **`CardViewModel`/`CreditViewModel`/`InvestmentViewModel`** — живы через легаси-EDIT-путь `FinanceAddAccountView` (Ф4 доказал: `isEditingLegacy`-гуарды несущие, снос воскресил бы баг Ф6a дублирования).
- **`FinanceAccountService`** — 11 живых сайтов (archive/restore/`physicallyDeleteLegacyAccount`), **`FinanceInvestmentOrderService`-класса живые мосты** (`CardCatalog`/`CardManager` — пикеры Cashflow/Cashback).
- **Core-периферия** — `ScopeMergeDedup/Reader/Types` (reconciliation), `DataIntegrityCleaner`, `DataResetService`, `SheetsDataMapper`, `TotalBalanceWidget`, `RateSourcePickerView`, `SheetsConnectionView`, `PortfolioHeldSymbolsProvider`, `AccountMarketPriceService` — каждый требует отдельной проверки живости/порта.
- **`InlineCreateForms.swift`** — DTO-рефактор (Ф3-находка, транзиентные `Card`/`Credit`/`Investment`-структуры без `insert`, типы нужно заменить на простые struct).
- **Схема V6 + снос @Model** — миграторы (`LegacyAccountsMigrator`/`GroupsMigrator`/`LegacyAccountConverter`/`LegacyConversionRegistry`) читают легаси-@Model напрямую → должны быть удалены ОДНОВРЕМЕННО с моделями (вариант «б» подтверждён: SwiftData делает вариант «а» недостижимым — @Model-тип нельзя оставить вне активной схемы и одновременно фетчить). Апгрейд-путь фиксируется: **старая версия → билд Ф1–Ф5b (миграция+ремапы уже произошли) → билд Ф5c** — на билде Ф5c миграторы уже не нужны (данные перенесены, ремап сделан), безопасно для 1–2 юзеров при явном апдейте через промежуточный билд.

**Решение:** Ф5c требует декомпозиции на под-фазы с собственными device-тестами (минимум: Cashback-порт → снос легаси-VM/менеджеров → Core-периферия → финальная схема+модели), а не выполнения «одним заходом» поверх уже сделанных 5a/5b. Продолжать сейчас означало бы жертвовать build/test-дисциплиной ради формального завершения — прямое нарушение self-audit чеклиста проекта.

---

## Вердикт по блокеру фазы 1 (для задачи №7 ночи)

**БЛОКЕРА НЕТ.** `LegacyAccountConverter` + `LegacyConversionRegistry` + `unconvert` уже существуют и покрыты тестами (Track C, 17 тестов, в develop); фаза 1 = идемпотентная одноразовая миграция + переключение чтения, **не удаляет ни одного файла**, полностью обратима (`unconvert`), потеря истории при opening-balance согласована владельцем (1–2 юзера). Единственная несцепка — гэп меты вклада/рынка в `Input` (мигрируют как manual-asset) — это допустимая деградация, не блокер.

## Редизайн экрана «Счета» — утверждён владельцем 2026-07-08, выполнять ПОСЛЕ сноса (Фаза 6)

Владелец одобрил дизайн-предложение (мокап: артефакт accounts-now-vs-proposal, https://claude.ai/code/artifact/98ee0fd4-c440-4169-8927-4d08a05fa100). Структурные пункты завязаны на единый AccountsCore, поэтому идут отдельной фазой ПОСЛЕ Фаз 1–5 (не красить сносимый код):

1. **Stacked-полоса «активы vs обязательства»** с нетто — вместо двух карточек Credit/Debit с прогресс-барами без шкалы.
2. **Секции «Активы» / «Обязательства» с подытогами** в списке групп — вместо смешанного списка.
3. **Шапка**: вторичная валюта чипом «≈ N $»; валютным группам подстрока «≈ в ₽». БЕЗ чипа прироста и БЕЗ sparkline.
4. **Кастомные иконки групп** (решение владельца 2026-07-09, см. ниже) — `AccountGroup.customIconName`, UI-пикер по паттерну `account-custom-icons`.

⚠️ **Решение владельца (2026-07-08): приросты и графики НЕ дублировать в «Счетах»** — «данные по приростам есть в Динамике и график», «лишнего городить не нужно». Экран «Динамика» уже показывает чип «+149 074 +1.6%», график тотала и разрез Groups с процентами. Поэтому из скоупа Фазы 6 ИСКЛЮЧЕНЫ: чип динамики/sparkline в шапке «Счетов» и «±X%» во второй строке групп (остаётся только «N счетов»). «Счета» = состояние (сколько где лежит), «Динамика» = движение (как менялось). Если когда-то захочется мостик между ними — максимум тап по группе → Динамика с фильтром этой группы, не дубль данных.

Быстрые правки того же ревью (НЕ ждут 6b, взяты в ночную полировку 2026-07-08): FAB-отступ списка; иконки типа продукта вместо цветных полосок + вторая строка «N счетов» (без процентов — см. решение выше); «Ungrouped» → локализованное «Без группы», нулевые группы → свёрнутые «Скрытые».

**Кастомные иконки групп — вход из ревью 2026-07-09, решение владельца: включить в Фазу 6.** Сейчас у `FinanceGroup`/`AccountGroup` нет поля под кастомную иконку — только `colorHex` и вычисляемая по доминирующему типу счетов иконка-бейдж (`FinanceRows.swift:203-227`, `FinanceGroupTypeIconView`). Это НОВАЯ фича, не баг — намеренно не втиснута в Фазу 1.5 (там только слияние моделей, не новый функционал). Ложится на `AccountGroup` (уже канон после Фазы 1.5), поле по готовому паттерну (`CashflowCustomCategory.icon` / `Account.customIconName`, `plans/2026-05-15__account-custom-icons.md`) — `customIconName: String?` (SF Symbol/эмодзи), UI-пикер переиспользует существующий `CashflowCategoryIconView`/аналог.

AC Фазы 6: соотношение полосы = данным тотала; подытоги секций сходятся с шапкой; вёрстка в токенах; RU/EN/zh-Hans; ноль дублей данных Динамики; кастомная иконка группы сохраняется и рендерится на всех 3 экранах (Счета/редактор группы/Динамика-breakdown), дефолт (без кастомной) — прежняя вычисляемая иконка по доминирующему типу.

## Журнал
- 2026-07-07: план создан (Максим/Plan, opus), инвентаризация по кодбазе; фаза 1 признана безопасной к ночной реализации.
- 2026-07-08 (ночь): добавлена Фаза 6 — редизайн экрана «Счета» (утверждён владельцем по мокапу); быстрые правки экрана вынесены в ночную полировку.
- 2026-07-08 (ночь, Александр): **Фаза 1 РЕАЛИЗОВАНА** на ветке `feature/legacy-accounts-purge` (от develop 6b0680d, НЕ мержено, НЕ пушено). Коммиты: мигратор+вайринг+тесты, фикс теста идемпотентности. 11 тестов зелёные, полный сьют 0 новых красных vs baseline (18). Отклонения: SwiftData-флаг→UserDefaults+archivedAt (schema V6 отдана Ф5), read-switch отдан Ф2 (инвариант держит двоемирие). Перед мержем — device stress-test + бэкап + «да» владельца.
- 2026-07-10 (ночь, Александр): **Фаза 2 РЕАЛИЗОВАНА (scoped)** на ветке `feature/legacy-accounts-purge` (коммит 80ad7cc, от 369c57c, НЕ мержено). Агрегат «Общий баланс» переведён на single-world ядро (`calculateTotalsSnapshot` core-only + заголовок Динамики получил вклад ядра — закрыт документированный gap unified-totals Ф4); `AccountTotalPolicy` снесён (инлайн). AC1/AC2 доказаны новыми тестами (`FinanceTotalsServiceFilterConsistencyTests`), 3 `FinanceViewModelTests` мигрированы на single-world, `AccountTotalPolicyTests` удалён. Build 0 ошибок, 0 новых красных vs baseline. **Escalation (mentor stress-test):** сдвинут в новую **Фазу 2b** доказанно-рискованный остаток — снос `mergingNewCoreSeries` (несущий stitching истории графика, ломает protected Dynamics-тесты + регрессит историю), унификация `FinanceRows`/`orderedAccounts` (инертна пост-миграции, орфанит Ф4-код), снос create-views (grep **опроверг** «мёртвый код» — референсятся FinancesSheets/FinanceAddAccountView, это Ф3/Ф4-писатели). `VM.calculateGroupTotal` оставлен bolt-on (без задвоения, схлопнётся при сносе легаси). Перед мержем — device stress-test + бэкап + «да» владельца.
- 2026-07-10 (ночь, Александр, адверсариальное ревью Ф2): **CONFIRMED-дефект исправлен** — transient «Общий баланс ≈ 0» на первом запуске (миграция бежала ПОСЛЕ `.ready`/монтирования RootTabView, core-only агрегат Ф2 видел пустое ядро, авто-пересчёта нет). Фикс — вариант (а): `runLegacyAccountsMigrationIfNeeded()` перенесён в `millioApp.swift`, вызывается ДО `useCase.initialize()`/`.ready`, внутри `onScopeResolved`. Тест `LegacyMigrationOrderingTests` (2, документирует баг + доказывает фикс). Побочно найден и задокументирован (не мой скоуп) пре-существующий крэш `GroupsMigratorTests` (Ф1.5, container-retention баг, ловится только через `xcresulttool`, НЕ через текстовый summary xcodebuild) — консольный `✔ Suite passed` признан ненадёжным индикатором.
- 2026-07-10 (ночь, Александр): **Фаза 2b РЕАЛИЗОВАНА** на ветке `feature/legacy-accounts-purge` (коммит `49fe371`, от `3a81230`, НЕ мержено). `GroupsMigratorTests` container-retention баг тем временем исправлен ДРУГИМ агентом (коммит `3a81230`) — подтверждён зелёным при гейте этой фазы. `ChartDataPoint.mergingNewCoreSeries` (dual-path: отдельный дневной ряд ядра + forward-fill приближённое поточечное слияние) снесён; замена — `addingCoreContribution` (точный `totalAt`-запрос на даты уже построенного легаси-скелета, легаси-скелет НЕ тронут). **Escalation-обоснование (mentor stress-test):** буквальная замена «единственный ряд из чистого core-only `seriesBetween`» отвергнута — доказано (по коду конвертера), что она стёрла бы РЕАЛЬНУЮ легаси-историю мигрированных пользователей (core-двойник датирован моментом миграции, не оригинальной датой счёта) — регрессия хуже документированного MVP-ограничения (то про искажение дельты, не про потерю графика). Реализована безопасная альтернатива: тот же комбинированный результат, без промежуточного массива/forward-fill. `ChartDataPointMergeTests` удалён (тестировал снесённую функцию), новый `FinanceDynamicsCoreContributionTests` (2) тестирует контракт через `updateChartDataAsync()`. 45 protected-тестов `FinanceDynamicsViewModelTests` НЕ переписаны (доказано: их схема без `Account` даёт core-вклад=0 идентично до/после). Гейты ТОЛЬКО через `xcresulttool`: Dynamics-сьют 45/45, новый сьют 2/2, полный `millioTests` 15 failed = 100% baseline, 0 новых красных. Остаток (FinanceRows/orderedAccounts унификация, VM.calculateGroupTotal core-only) переименован в **Фазу 2c** (не начата).
- 2026-07-10 (ночь, Александр): **Фаза 3 РЕАЛИЗОВАНА (scoped)** на ветке `feature/legacy-accounts-purge` (коммит `52fff71`, от `49fe371`, НЕ мержено). `StockBulkImportServices.persist(...)` переведён на `AccountsCoreService` (единственная запись `.marketInvestment` + `buy()`), легаси `Investment`/`FinanceAccount` больше не создаются; `loadLocalCatalog()` расширен дуальным чтением. 3 теста переписаны на core-Account. **Escalation-находки (mentor stress-test — все доказаны кодом, не предположением):** (1) `ScreenshotDataSeeder` НЕ мигрирован — доказанный блокер: `Cashback.cardIDs` резолвится строго против легаси `Card` (`CashbackViewModel.swift:594-606`), перевод сидов на `Account` молча обнулил бы cashback-скриншоты (Cashback нигде не в скоупе плана 6b); файл безопасен для юзеров (`isScreenshotMode`-only), эскалировано владельцу — судьба `Card.swift` в Ф5 зависит от решения по Cashback. (2) `FinanceCreateViews.swift` подтверждён ПОЛНОСТЬЮ мёртвым (не просто «референсится», как нашла Ф2) — триггер (`showCreateCardSheet`/etc.) нигде не вызывается, снос — явный скоуп Ф4. (3) `InlineCreateForms.swift` — живой, но НЕ писатель (Card/Credit/Investment там — транзиентные DTO для биндинга, ноль `modelContext.insert`, реальная запись через `AccountsCoreService` в `FinanceAddAccountView`); буквальный grep-гейт формально не чист, но инвариант (запись) не нарушен — рефакторинг DTO-типов отложен в Ф4 (координировать со сносом моделей). Build 0 ошибок; `StockBulkImportTests` 24/24 (xcresulttool); полный `millioTests` 22 failed = 16 baseline + 1 LanguageManager-flaky + 5 нового задокументированного flaky-класса (`UserDefaults.standard` rate-cache leak, доказано изоляцией — не регрессия Ф3, зафиксировано в `progress/accounts-core-baseline-failures.md`). Перед мержем — device stress-test + бэкап + «да» владельца (трогает единственный писатель bulk-импорта акций).
- 2026-07-10 (ночь, Александр): **Фаза 4 РЕАЛИЗОВАНА (scoped)** на ветке `feature/legacy-accounts-purge` (коммит `5221736`, от `25cfd6d`, НЕ мержено). Снесена доказанно-мёртвая легаси-редактор-поверхность: `CardEditorView`/`CreditEditorView`/`InvestmentEditorView` + `FinanceEditViews`/`FinanceEditorWrappers`/`FinanceCreateViews` + осиротевший `InvestmentEditorLayoutPolicyTests` + мёртвая обвязка в `FinanceViewModel` (state/actions/reducer `showCreate*`/`showEdit*`/`editing*ID`/`editAccount()`) + 6 sheet-блоков `FinancesSheets` (−3054 строк). Достижимость: `.editAccount`/`.showCreateCardSheet` НИГДЕ не диспатчатся (мёртвый action-case). `DepositCashflowBridge` верифицирован чистым. Тест-гард `AllPresetsOnNewCoreTests.editingLegacyReturnsNilForEveryResolver`. **Escalation (mentor stress-test — всё доказано грепом/кодом, file:line):** (1) гуарды `isEditingLegacy` НЕ удалены — посылка §4 «мёртвые» ОПРОВЕРГНУТА: легаси-EDIT-путь `FinanceAddAccountView` жив через `FinanceDynamicsView.showFullProductEditSheet`, снос воскресил бы баг Ф6a. (2) `AccountsCoreCashflowBridge` НЕ упрощён — легаси-ветки несущие до Ф5 (миграция не перепривязывает `CashflowTransaction.cardID`, историч. операции на легаси-ID, легаси `Card` жив до Ф5; nil-резолюция защищает от дубля события). (3) `CardCatalog`/`CardManager`/`FinanceAccountService`/`FinanceInvestmentOrderService` НЕ снесены — живут в God-VM/Cashback (пикеры Cashflow/Cashback, управление архивными легаси, инвест-ордера на легаси `Investment`). (4) легаси-VM + `InlineCreateForms` DTO → Ф5 (живут через EDIT-путь + транзиентные DTO, трогают живой экран добавления). **Cashback-узел (AC4):** сквозная зависимость всей фичи от легаси `Card` (пикер/`cleanInvalidCardIDs`/backup-import/фильтры) — безопасный фикс = ВЫДЕЛЕННАЯ миграция Cashback→ядро (user-data, device stress-test); `LegacyConversionRegistry.coreAccountID` даёт мэппинг для будущей перезаписи `cardIDs`. Остаточный блокер Ф5: `Card.swift` живёт как spec-исключение (рекомендация б), нужно решение владельца. Build 0 ошибок; полный `millioTests` 1709 passed / 26 failed = baseline/flaky (вкл. новый order-flaky `GroupsMigratorTests.duplicateLegacyGroupNames…`, run1 зелёный/run2 красный на одном коммите — файл Ф4 не тронут), 0 новых красных (xcresulttool).
- 2026-07-09: ревью плана (сессия ревью, sonnet). Добавлена **Фаза 1.5 — слияние моделей групп** `FinanceGroup`↔`AccountGroup` (вход из `plans/2026-07-05__unified-totals.md` §1.3a, найдено владельцем на симуляторе). Верифицировано субагентами: диагноз §1.3a актуален (file:line подтверждены), у групп нет поля иконки. **Решения владельца:** (1) Фаза 1.5 — отдельная фаза между Ф1 и Ф2; (2) все три поля `isFavorite`/`usesManualAccountOrdering`/`priorityRaw` переносятся при слиянии; (3) кастомные иконки групп (новая фича) — в Фазу 6 (редизайн Счетов), не раньше. План готов к следующему шагу — реализации Фазы 1.5 (по-прежнему под guard phrase, код не пишется без явной команды).
- 2026-07-10 (Александр, guard phrase снята владельцем явно): **Фаза 5a РЕАЛИЗОВАНА** (коммит `7d5e906`, от `5221736`) и **Фаза 5b РЕАЛИЗОВАНА (scoped)** (коммит `c7ef57a`) — детали в секциях фаз выше. Владелец утвердил решение по ремапу (вариант 1 — `LegacyConversionRegistry` legacy→core, атрибуция сохраняется) и разбивку 5a/5b/5c одним заходом без пауз. **Фаза 5c ОСТАНОВЛЕНА ментором до старта** — свежий пост-5a/5b grep показал 76 файлов, всё ещё ссылающихся на легаси-типы, из них минимум 5 — самостоятельные M-размера под-проекта (Cashback-порт ~3500 строк, снос легаси-VM редакторов, Core-периферия ~9 файлов, InlineCreateForms DTO-рефактор, финальная схема+модели). Продолжение «одним заходом» нарушило бы build/test-дисциплину проекта (полный гейт возможен только после честной декомпозиции с device-тестами на каждой под-фазе). Обе реализованные под-фазы (5a/5b) прошли собственный build-гейт (SUCCEEDED), закоммичены отдельно, план и sidecar актуализированы для продолжения в следующей сессии.

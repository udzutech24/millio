# План: Ф5c.7 — реплатформинг Finances-слоя на AccountsCore (слито с Ф6)

**Дата создания:** 2026-07-11 · **Статус (обновлено 2026-07-11 вечер):** 5c.7.0–5c.7.3 РЕАЛИЗОВАНЫ, Fable-подтверждены, закоммичены в develop локально (7 коммитов поверх `dda6b5e`, НЕ запушено). 5c.7.4+5c.7.5 (объединённый type-flip) — код НЕ начат, после риск-калибровки владельца Александр провёл ещё один research-проход и рекомендует **expand-contract вместо blind atomic flip** (см. «Второй стоп» ниже) — **передано в НОВЫЙ ЧАТ**, эта сессия слишком длинная для продолжения. 5c.7.6/5c.7.7 — далее по плану, зависят от expand-contract. · **Статус (обновлено 2026-07-11 ночь, новая сессия):** EXPAND-ФАЗА ВЫПОЛНЕНА — 9 коммитов `a5169a7→f88abd2` (локально, НЕ запушено): expand-шаг + миграции №1/№2/№3/№5 (№4/№6/№7 — обоснованные no-op), Ungrouped-фикс, Gate A (валютная разбивка Динамики), Gate B (characterization-замок updateAccountAmount). Каждый гейт — независимая Fable-верификация (3 отклонения → фиксы → CONFIRMED). Остался CONTRACT-шаг (атомарный флип по каталогу из журнала ниже) + 5c.7.6 + 5c.7.7 — следующей сессией. **Решения владельца (2026-07-12):** паволл-фикс — sign-off получен, реализуется Gate C этой же сессией; `convertAccountToCore`/`unconvert` — ОСТАВИТЬ (владелец сам удалит/перенесёт свои легаси-счета, снос узла — после ручной очистки, при флипе НЕ сносить). · **Статус (обновлено 2026-07-11/12, продолжение сессии):** CONTRACT-ШАГ ВЫПОЛНЕН — атомарный флип `FinanceViewModel`/`FinanceDynamicsViewModel`/`FinanceGroupService`/7 Views на `AccountGroup`/`Account`, коммит `871dfc0` (локально, НЕ запушено). Терминальный гейт (build+protected-кластер+FilterConsistency+characterization+mixed-store+Gate B+UI smoke-launch) — Fable CONFIRMED, включая повторную точечную проверку 2 post-review фиксов (деталь — журнал contract-шага ниже). PENDING: интерактивный UI-walkthrough в симуляторе (заблокирован Accessibility permissions среды), 3 adversarial-находки не проверены глубоко (не блокеры). 5c.7.6/5c.7.7 — следующим шагом. · **Тип:** L (2-3 сессии) · **Родитель:** `plans/2026-07-07__legacy-accounts-purge-path-b.md`, секция «⛔ Блокер Ф5c.6», remaining-scope 5c.7. Разблокирует Ф2c (унификация `orderedAccounts`/`calculateGroupTotal`/per-account chart-modes) и открывает путь к 5c.8–5c.11 (снос @Model).

**Baseline для всех гейтов этого плана:** `xcrun xcresulttool` **1745 passed / 14 failed** (develop @ `aea084a`, HEAD на момент старта Ф5c.7 — `dda6b5e`, docs-only поверх `aea084a`, baseline не меняется). Любая под-фаза обязана дать 0 новых красных относительно этого числа — **сравнивать список идентификаторов упавших тестов, не только счётчик** (Fable-риск №10: миграция одного из 14 baseline-красных на другую причину не должна маскироваться как «то же самое число»).

> **Fable-ревью §0 (2026-07-11, до старта кода)** — независимая проверка plan-review + стресс-тест. Полный текст риска/митигации ниже по тексту рядом с соответствующей под-фазой. Верифицированы все file:line из §1 (актуальны); 3 минорных расхождения без влияния на план: `AccountEventType` — 15 case, не 14; `createMoneyAccountOnNewCore` def `:1121`/call `:1263` (не `:1275`); FDVM-метод называется `orderedAccounts(in:)`, не `(for:)`. Два содержательных вывода учтены ниже (5c.7.0 и 5c.7.1 скорректированы).

> **⚠️ МЕТОДОЛОГИЯ ГЕЙТОВ — обновлено после верификации 5c.7.0 (2026-07-11).** Заголовочный baseline **1745/14 устарел и неточен** — независимый прогон Fable-верификатора на том же коммите `dda6b5e` дал **1771/14**, расхождение 26 тестов, плюс сам сьют флакует ±1-2 между последовательными прогонами без каких-либо изменений (параллельный запуск/shared-state, не архитектурная проблема этого плана). Дальше по плану применяется **скорректированный протокол гейта**, а не сравнение с числом 1745/14:
> 1. Перед каждым гейтом — свежий прогон на ЧИСТОМ HEAD без диффа под-фазы (`git stash -u` → xcresulttool → `git stash pop`) — это и есть baseline для этого конкретного гейта, не число из шапки.
> 2. Сравнивать **сет ID failed-тестов**, не счётчик.
> 3. Любой ID, которого нет в свежем clean-baseline сете, но есть в прогоне с диффом — не считать красным гейтом сразу: перепрогнать этот ID изолированно (`-only-testing`) 1 раз. Зелёный изолированно → флак, не блокирует. Красный изолированно дважды подряд → настоящая регрессия, стоп-правило.
> 4. Это не ослабление гейта — фактическая строгость та же (0 новых детерминированных красных), просто протокол сравнения починен под реальное поведение сьюта.

> **⚠️ КАЛИБРОВКА РИСКА — уточнено владельцем (2026-07-11, вечер).** Millio на этом этапе фактически имеет одного реального пользователя — самого владельца, тестирующего на своём устройстве (pre-launch, публичной базы пользователей нет). Это меняет цену ошибки: «риск для пользовательских финансовых данных» из формулировок ниже по факту равен риску для данных владельца, которые восстановимы из CloudKit-бэкапа, а не риску для множества сторонних юзеров. **Следствия для остатка плана:**
> 1. Требование «отдельная сессия + обязательный физический device-тест перед КАЖДЫМ гейтом» — снято как хард-блокер. Тестовая дисциплина (build 0 ошибок, xcresulttool-протокол выше, Fable-независимая верификация каждого гейта) остаётся ПОЛНОСТЬЮ — она ловила реальные архитектурные находки весь день (дубль Ungrouped, мёртвый код, дизайн-баг с обнулением тотала) и не связана с числом пользователей.
> 2. Device-тест — по-прежнему полезен (это всё ещё реальные данные владельца), но статус PENDING больше не блокирует переход к следующей под-фазе или коммит — симуляторный эквивалент + Fable-подтверждение гейта достаточны для продолжения в течение дня.
> 3. Push/merge/deploy — правило не меняется, по-прежнему только с явного «да» отдельно от разрешения продолжать код.** rich-редактирование существующего счёта (rename/currency/группа/мета/иконка) недоступно юзеру с Ф1 — `AccountsCoreService` не имеет `updateAccount`, легаси rich-editor снесён как runtime-мёртвый (`1b4e60f`). Решение — строить в под-фазе 5c.7.0 (см. ниже), это не регрессия, а закрытие давнего пробела.

---

## 1. Research — что реально зависит от легаси (file:line)

### 1.1 `FinanceViewModel.swift` (1874 строк, 68 легаси-строк, живая — вход `RootTabView.swift:337`)

| Роль | Символ | Легаси-тип |
|---|---|---|
| Published state | `state.groups` | `[FinanceGroup]` |
| Published state | `availableCards/Credits/Investments` | `[Card]`/`[Credit]`/`[Investment]` |
| Кэш | `cardByID/creditByID/investmentByID` | `[String: Card/Credit/Investment]` |
| Экшен | `editGroup(FinanceGroup)`, `removeAccountFromGroup(FinanceAccount)`, `showAccountDynamics(FinanceAccount)` | параметры легаси |
| Экшен (data-critical) | `.physicallyDeleteLegacyAccount(FinanceAccount)` — case `:174`, handle `:499-500`, impl `:1332-1333` → `accountService.physicallyDeleteLegacyAccount` | вызывается из `FinanceDynamicsView.swift:1445` (purge-footer) |
| Расчёт (Ф2c-блокер (в)) | `calculateGroupTotal(group: FinanceGroup, …)` `:892` | ключён на легаси, схлопнётся сам после сноса, НЕ трогать напрямую |
| Мост | `orderedAccounts(for:)` `:970` — 1 из 3 определений (см. §1.3), `FinanceGroup → [FinanceAccount]` |
| Мост | `legacyAccountConverter`, `resolveCoreGroup` — уже существующие точки конвертации легаси↔core (переиспользовать, не изобретать заново) |

Потребители во View (через `getAccountInfo`/`FinanceAccountRow`, 8 файлов): `FinanceQuickEditAccountView`, `FinanceDynamicsView`, `FinanceOverviewCardView`, `FinanceGroupEditorView`, `FinancesSheets`, `FinanceRows` (+2). Уже на core: дуальный рендер `FinanceRows` (легаси) + `newCoreAccounts` (core) сосуществуют на экране «Счета».

### 1.2 `FinanceDynamicsViewModel.swift` (2915 строк, 57 легаси-строк, живая — экран «Динамика»)

- Fetch: `FetchDescriptor<Card/Credit/Investment>()` `:523-529` — единственная точка чтения легаси в этом файле.
- State/кэш: `state.groups: [FinanceGroup]`, `cardsCache: [String: Card]`.
- Per-account chart-modes (byAccounts/singleAccount, Ф2c-блокер «1b») живут внутри этого God-VM, завязаны на тот же fetch.
- **Уже реализован механизм «один источник правды» (6b, инвариант держит тест):** `aggregateGroupRows`/`signedAccountValue` — per-account строки считаются один раз, `.groups` = агрегация тех же строк; `addingCoreContribution` заменил снесённый `mergingNewCoreSeries` (stitching истории графика core-вклада, без задвоения). Инвариант `Total(Groups) == Total(Accounts)` закреплён тестом **`FinanceTotalsServiceFilterConsistencyTests`** (AC1/AC2) — **нельзя сломать**.
- `coreContributionWithLegacyPredecessor`/`legacyPredecessorContribution` — общий путь Cashflow/Dynamics для стартового Assets-at-period (багфикс 2026-07-10, `86e9918`) — трогать только через этот общий путь, не дублировать.
- Protected Dynamics-тесты — 6 файлов, ~3347 строк (полный сьют этого кластера — держит регресс графика/breakdown); прогонять после каждого шага 5c.7.4.

### 1.3 `FinanceAccountService.swift` (568 строк, 47 легаси-строк) / `FinanceGroupService.swift` (292 строки, 23 легаси-строки)

Публичные методы `FinanceAccountService` с легаси-типами: `loadAccounts()`, `addAccountToGroup(accountType:accountID:group:FinanceGroup?)`, `removeAccountFromGroup(_:FinanceAccount)→UnderlyingAccountKind`, `deleteAccountPermanently(_:FinanceAccount)→UnderlyingAccountKind`, `legacyRelatedTransactionCount(for:FinanceAccount)→Int`, `physicallyDeleteLegacyAccount(_:FinanceAccount)`, `restoreArchivedAccountToGroup(...)`, `updateUnderlyingArchiveState(...)×2`. Приватные: `normalizeCreditsIncludeInTotal`, `normalizeMarketAssetIdentities/QuoteLookupKeys`, `rebuildAllAccountCaches`, `cleanupInvalidFinanceAccounts`, `resolveUngroupedGroup()→FinanceGroup`.

**Важная граница скоупа:** `FinanceAccountService.swift:107/146` также инициализирует `CardCatalog`/`CardManager` — эти кэши потребляются Cashflow/Cashback-пикерами (`CashflowViewModel`, `CashbackViewModel`), НЕ Finances-слоем напрямую. Это отдельный узел (5c.6-кластер/будущая Cashback-миграция, вне 5c.7). **5c.7 переносит типизацию FinanceAccountService/FinanceGroupService на `Account`/`AccountGroup`, но НЕ трогает populate-логику `CardCatalog`/`CardManager`** — она либо остаётся как узкий residual-метод внутри сервиса (минимальный диф), либо переезжает в отдельный маленький сервис, если типы разойдутся. Решается на месте в 5c.7.2, не заранее.

`FinanceGroupService`: `visibleGroupsForList()→[FinanceGroup]`, `orderedAccounts(for:)` (1 из 3 определений — второе, третье в `FinanceViewModel:970`/`FinanceDynamicsViewModel:2868`), `deleteGroup`, `updateGroup`, `moveGroup`, `normalizeHiddenGroupOrders`, `nextAccountOrder`, `syncCoreGroup(oldName:newName:colorHex:displayCurrency:)` (уже существующий мост легаси-имя→core-группа, переиспользовать).

**Найден механизм дубля «Ungrouped» (гипотеза агента, подтверждена чтением):** `FinanceGroupService.swift:75-81` — `shouldHideGroupInList` скрывает легаси-Ungrouped-группу ТОЛЬКО когда `visibleAccountsForGroup(group).isEmpty && coreAccountsCount(group) == 0`. Если на core-стороне позже появляется свой «Ungrouped»-эквивалент (`AccountGroup`) — это будет ВТОРАЯ видимая пустая группа с тем же именем: `AccountGroup`/`AccountsCore` **не имеет** системного Ungrouped-эквивалента (grep `system|Ungrouped|ungrouped` по `millio/Core/AccountsCore` — 0 хитов). Единственный текущий Ungrouped — легаси `FinanceGroup` с именем `ungroupedGroupName`. Значит дубль возникает не от двух Ungrouped-сущностей сейчас, а от рассинхрона видимости легаси-группы против core-счетов без группы, отображаемых отдельным блоком в `FinanceRows` (дуальный рендер). **5c.7.1 обязан ввести ОДИН канонический Ungrouped на стороне `AccountGroup`** (или явный «no group» statuс на `Account`) и убрать легаси `resolveUngroupedGroup()`/`shouldHideGroupInList`-развилку — это устраняет баг структурно, не патчем видимости.

`AccountsCoreAdditionBridge.resolveAccountGroup(matching financeGroup: FinanceGroup?)` `:66` — вызывается `QuickSetupApplier:135`, `FinanceAddAccountView` ×4 — существующий мост легаси-группа→core-группа при CREATE, переиспользовать как образец для 5c.7.1.

### 1.4 `AccountsCoreService` (560 строк) — API-карта + гэп rich-edit

18 public-методов: `createAccount`, `recordEvent`, `adjustBalance`, `buy`/`sell`, `revalue`, `upsertEvent`, `transfer`, `archiveAccount`/`restoreAccount`, `physicallyDelete` и т.д. (event-sourcing, 14 `AccountEventType`). **`updateAccount` подтверждённо отсутствует** (grep `updateAccount|rename|editAccount|updateMeta|setName` по `millio/Core/` = 0 хитов, перепроверено). `AccountDetailView` (`FinanceRows.swift:394`) правит ТОЛЬКО баланс (`adjustBalance` `:505/728`) и архив (`archiveAccount` `:818`). `createMoneyAccountOnNewCore` (`FinanceAddAccountView.swift:1275`) — CREATE-only, доказанно непригоден для EDIT (bug Ф6a при переиспользовании).

Легаси rich-EDIT снесён коммитом `1b4e60f` (2026-07-11, обоснованно — путь был runtime-мёртв для мигрированного юзера): `FinanceDynamicsView.fullProductEditSheetContent`/`showFullProductEditSheet`/резолверы `resolvedCard/Credit/Investment`; `FinanceAddAccountView.editingCard/Credit/Investment`/`isEditingMode`/`makeLegacyCard`+6 легаси-writers. Три легаси-VM (`CardViewModel`/`CreditViewModel`/`InvestmentViewModel`) НЕ удалены — они backing живой CREATE-формы (`InlineCreateForms.swift`), декаплинг которых отдан в 5c.7.5 (было отложено в Ф6 по плану-родителю).

---

## 2. Spec — целевое поведение

### 2.1 Инварианты, которые нельзя сломать
1. **`Total(Groups) == Total(Accounts)`** — held by `FinanceTotalsServiceFilterConsistencyTests`. Любой рефакторинг group-агрегации обязан гонять этот тест на каждом шаге 5c.7.1/5c.7.4.
2. **Один источник правды breakdown**: per-account строки считаются один раз, group-агрегация — производная (`aggregateGroupRows`-паттерн), не отдельный расчёт. Новый `Account`-based код обязан следовать этому же паттерну, не вводить второй параллельный путь агрегации.
3. **`isEditingLegacy`-гуард моста** (`AccountsCoreAdditionBridge`) остаётся — защищает от дубликата при повторном использовании CREATE-пути для EDIT (тест `AllPresetsOnNewCoreTests:157-183`).
4. **Protected Dynamics-тесты** (6 файлов ~3347 строк) — 0 новых красных на каждом гейте 5c.7.4/5c.7.6.
5. **Легаси archive/restore/physicallyDeleteLegacyAccount продолжают работать** для оставшихся немигрированных легаси-записей (если есть) до момента, пока `Card`/`Credit`/`Investment` @Model физически существуют (до 5c.11) — 5c.7 переносит ТИПИЗАЦИЮ VM/сервисов на core, не удаляет @Model и не обрывает fallback для граничных случаев.
6. **`CardCatalog`/`CardManager` populate-логика** (Cashflow/Cashback-потребители) не регрессирует — переносится/сохраняется как отдельный узел, не как побочный эффект типовой миграции.
7. **[Fable-ревью] Backup/restore roundtrip.** Новые события/поля (`updateAccount`, `AccountGroup.customIconName`) обязаны переживать CloudKit backup→restore без потери и без краша на старом бэкапе, снятом до появления этих полей. Roundtrip-тест — часть гейта 5c.7.0 (для updateAccount-событий) и 5c.7.6 (для customIconName).
8. **[Fable-ревью] Смена валюты счёта в v1 `updateAccount` — ЗАПРЕЩЕНА.** Прямая мена валюты мутацией истории событий = переинтерпретация сумм задним числом (100000 RUB молча читается как 100000 USD) = порча пользовательских данных. 5c.7.0 разрешает менять name/группу/мета/иконку; смена валюты — либо отдельное `revalue`-событие с явной конвертацией (вне скоупа 5c.7.0), либо явный disabled-стейт в UI. Не реализовывать смену валюты как побочный эффект rename-формы.
9. **[Fable-ревью] Mixed-store как явный тест-кейс.** 5c.7.3/5c.7.4 обязаны иметь fixture с ОДНОВРЕМЕННО core-счетами и непроконвертированными legacy-записями (не только «чистый core» или «чистый legacy») — иначе `FetchDescriptor<Account>`-миграция тихо теряет legacy-хвост из графика/breakdown/пикеров.

### 2.2 Целевое поведение — экран «Счета» / «Динамика» после 5c.7
- `FinanceViewModel`/`FinanceDynamicsViewModel` читают `[Account]`/`[AccountGroup]` как первичный источник; легаси-@Model остаются только как fallback-чтение для непроконвертированных edge-case записей (если такие в принципе остаются после Ф1 — по факту это должно стремиться к нулю для реальных юзеров).
- Один канонический Ungrouped на стороне `AccountGroup` — устраняет дубль.
- `orderedAccounts`/`getAccountInfo`/`FinanceAccountRow`-каскад — единая core-типизированная реализация вместо 3 легаси-дублей.
- Rich-edit существующего счёта (rename/currency/группа/мета/иконка) — доступен юзеру через `AccountDetailView` + новый `AccountsCoreService.updateAccount` (event-sourced: имя/валюта/мета/группа как отдельные события или прямые поля-мутации на `Account`, согласовать с существующей event-моделью, не ломая `adjustBalance`/`archiveAccount`).
- Экран «Счета» (Ф6, слито сюда): stacked-полоса активы/обязательства с нетто, секции «Активы»/«Обязательства» с подытогами, `AccountGroup.customIconName` (уже найден паттерн `AccountIconPickerSheet`/`FinanceOverviewLedgerPresentation` — переиспользовать, поле добавляется additive без V6-бампа схемы). Без чипа прироста/sparkline в шапке «Счетов» (решение владельца 2026-07-08 — не дублировать Динамику).

---

## 3. План по под-фазам (гейт на каждой: build 0 ошибок; `xcrun xcresulttool` 0 новых красных vs baseline 1745/14; device stress-test — только для фаз, трогающих архив/удаление/реальные пользовательские данные; `/stress-test` + явное «да» владельца перед мержем каждой под-фазы, как во всех предыдущих фазах плана-родителя)

### 5c.7.0 — `AccountsCoreService.updateAccount` API + минимальный вход (M, фундамент, закрывает открытый вопрос владельца) — [x] РЕАЛИЗОВАНО, Fable-подтверждено, закоммичено (2026-07-11)
> updateAccount — прямая мутация полей + save (не событие, обоснованно проще); иконка исключена из скоупа (нет backing-поля в ядре, добавление было бы мёртвым кодом — переносится в 5c.7.6 вместе с `AccountGroup.customIconName`); инвалидация кэша при смене группы не потребовалась (кэш не group-keyed, доказано тестом AC3). Device-тест редактирования 3 типов счетов — PENDING (нужен телефон владельца вечером).
- **[Fable-ревью, скоуп сужен]** Спроектировать и реализовать `updateAccount` (имя/группа/мета/иконка — **БЕЗ смены валюты**, см. §2.1 инвариант 8) на event-sourced модели, согласованно с `adjustBalance`/`archiveAccount`.
- Минимальный UI-вход в `AccountDetailView` (простая форма rename/группа/мета, НЕ полноценный `FinanceAddAccountView`-based rich-editor).
- **Полноценный rich-edit UI (переиспользование `FinanceAddAccountView` в режиме prefill-from-core) — ПЕРЕНЕСЁН в 5c.7.5**, после того как эта форма перетипизирована на core (иначе двойная работа: строить prefill на легаси-типизированной форме, потом перетипизировать её же в 5c.7.5 — риск реинкарнации дубликат-бага Ф6a, если prefill трогает CREATE-путь раньше времени).
- Тест: create→updateAccount(rename/group/meta)→verify, не создаёт дубликат (регресс-guard по аналогии с `AllPresetsOnNewCoreTests`).
- Тест: backup→updateAccount-событие→restore roundtrip (инвариант 7).
- Гейт: build+xcresulttool; device-тест редактирования 3 разнотипных счетов (card/credit/investment-эквивалент на core) — минимальной формы, не финального UI.

### 5c.7.1 — `FinanceGroupService` → `AccountGroup` (S/M) — [x] РЕАЛИЗОВАНО, Fable-подтверждено, закоммичено (2026-07-11); device PENDING
> **Fable-верификация (независимая).** Все 5 пунктов CONFIRMED, включая правомерность ментор-отсрочки type-flip (п.1) и легитимность фикса root cause (п.2). **Резидуальная находка, не блокер, занесена в скоуп 5c.7.3:** guard в `AccountsCoreAdditionBridge` не покрывает два обходных пути, создающих `AccountGroup` мимо моста — `LegacyAccountsMigrator.resolveCoreGroup:289-296` и `FinanceViewModel.resolveCoreGroup:1443-1453` — оба гардят Ungrouped только по локали текущего языка, кросс-локальный легаси-Ungrouped там всё ещё может материализоваться как отдельная core-сущность. Пре-существующий (не внесён этим диффом). 5c.7.3 обязана унифицировать оба `resolveCoreGroup` на мост с широким `allKnownUngroupedNames`-гардом — это же закрывает хвост.
> **Журнал (2026-07-11, Александр).** Разбор при реализации изменил трактовку под-фазы против буквальной формулировки — зафиксировано как ментор-находка:
> 1. **Type-flip публичного API `FinanceGroupService` → core ОТЛОЖЕН на 5c.7.3/4/5.** Доказано file:line: весь публичный API (`visibleGroupsForList()→[FinanceGroup]` ← FVM:966 ← FinancesView:539; `orderedAccounts(for:FinanceGroup)→[FinanceAccount]` ← FVM:970 ← FinanceGroupRow; все конструктор-замыкания) потребляется легаси-typed `state.groups`/`FinancesView`. Смена типа листа = каскад в 3 отложенные фазы одновременно. По явному escape-hatch владельца («если требует правки VM — отложи») — не тянул.
> 2. **Дубль Ungrouped устранён в КОРНЕ, не косметикой `shouldHideGroupInList`.** `shouldHideGroupInList` (`:79-81`) — легаси-патч видимости легаси-списка, ортогонален core-дублю; его снос лишь всегда-показал бы пустой легаси-Ungrouped (регресс), не фикс. Реальный источник «отдельной core-сущности Ungrouped» = `AccountsCoreAdditionBridge.resolveAccountGroup` создавал `AccountGroup` с именем Ungrouped, когда `QuickSetupApplier:131-135` слал в него легаси-Ungrouped-группу (`GroupsMigrator:85` гардит вручную, а bridge — нет). **Фикс: централизованный guard в `resolveAccountGroup` → `nil` для empty/`allKnownUngroupedNames`.** Инвариант «Ungrouped = group==nil» теперь не зависит от памяти каждого вызывающего.
> 3. **`orderedAccounts`: 3 «дубля» преувеличены.** FVM:970 — уже тонкий делегат в сервис (не дубль). FDVM:2868 `orderedAccounts(in:)` — реальный второй расчёт, но приватный + завязан на `state.showArchivedAccounts`/`getAccountInfoForDynamics` (VM-specific) → унификация требует правки God-VM → **отложено на 5c.7.4**.
> 4. **Тесты:** +2 bridge (`resolveAccountGroupReturnsNilForUngroupedSystemGroup`/`ForEmptyName`), +3 CRUD (`FinanceGroupServiceCRUDTests`: create/rename+syncCoreGroup/move), обновлён 1 characterization (`QuickSetupApplierTests.testApplyFallsBackToUngrouped…:121` кодировал анти-инвариант `group?.name==Ungrouped` → теперь `group==nil`). Гейт: build 0 ошибок; `FinanceTotalsServiceFilterConsistencyTests` зелёный; clean-baseline (git stash) vs diff — 0 новых детерминированных красных (`GroupsMigrator/duplicateLegacyGroupNames…` — пре-существующий флак SwiftData fetch-order, падает и на clean baseline).
- **[Fable-ревью, скорректировано] Канонический Ungrouped = `account.group == nil`, НЕ отдельная `AccountGroup`-сущность.** `AccountsCoreAdditionBridge.swift:64-65` уже фиксирует эту семантику ядра («счёт без группы = nil, AccountGroup не создаётся») — создание отдельной Ungrouped-сущности `AccountGroup` ей противоречит и породило бы вторую видимую пустую группу вместо устранения дубля (Fable-риск №4, вероятность высокая при исходном варианте). Убрать легаси `resolveUngroupedGroup()`/`shouldHideGroupInList`-развилку, ввести единую точку рендера «счета без группы» поверх `group == nil`.
- `orderedAccounts(for:)` — одна core-реализация вместо 3 легаси-определений; `syncCoreGroup` как образец моста.
- Гейт: `Total(Groups)==Total(Accounts)` тест зелёный; group CRUD device-тест (создать/переименовать/удалить/переместить группу).

### 5c.7.2 — `FinanceAccountService` → `AccountsCoreService`-обёртка (M) — [x] ГЕЙТ РЕАЛИЗОВАН (тесты-инварианты), тип-флип/core-маршрут ОТЛОЖЕН на 5c.7.3 (ментор-отсрочка), Fable-верификация PENDING, НЕ закоммичено (2026-07-11, Александр)
> **Журнал (2026-07-11, Александр) — ментор-отсрочка с доказательствами file:line.** Разбор при реализации показал, что буквальный «перевод 5 CRUD-методов на `Account`/`AccountsCoreService`» в 5c.7.2 НЕ верен по двум независимым причинам, каждой достаточно:
> 1. **Каскад (симметрично 5c.7.1).** Все вызывающие передают легаси-типы: `FinanceDynamicsView.swift:1413` (`.removeAccountFromGroup(FinanceAccount)`), `:1445` (`.physicallyDeleteLegacyAccount(FinanceAccount)`); `FinanceGroupEditorView.swift:589`; экшены `FinanceViewModel.swift:169-174` (типы `FinanceAccount`/`FinanceAccountType`/`FinanceGroup?`), делегаты `:1310-1350`. Смена сигнатур `FinanceAccountService` на core = одновременный тип-флип публичных экшенов VM (5c.7.3) + View (5c.7.5). Escape-hatch владельца → отложено на 5c.7.3, где VM всё равно перетипизируется.
> 2. **Core-маршрут был бы МЁРТВЫМ кодом + дублем (Challenge Loop #3).** Core-путь архива/restore/delete УЖЕ существует и живой для мигрированного юзера: `AccountDetailView.swift:846` → `archiveAccount`, `ArchivedAccountsView.swift:133/144` → `restoreAccount`/`physicallyDelete`. А `FinanceAccountService` CRUD достижим ТОЛЬКО для активных легаси-счетов (немигрированные leftover): миграция прячет легаси через `archivedAt` (`LegacyAccountsMigrator.swift:96/106/116`, converter создаёт core-двойник + `archivedAt` на легаси, двоемирие до 5c.11), поэтому у счёта, попадающего в эти методы, core-двойника нет по построению → ветка `AccountsCoreService.archive...` не сработала бы никогда. Маршрут дублировал бы `AccountDetailView:846`/`ArchivedAccountsView:133/144`.
> **Итог:** `FinanceAccountService` CRUD оставлен как легаси-fallback-узел (инвариант 5 §2.1), `loadAccounts()` — как residual CardCatalog/CardManager populate (инвариант 6 §2.1, §1.3-граница), обе группы БЕЗ правок продакшена. Реальный дедливерабл 5c.7.2 = его же явный гейт (два теста-инварианта), которые фиксируют выживание легаси-пути + populate-узла под окружающий реплатформинг. Это ценнее буквального порта: неявное допущение стало проверяемым тестом.
> **Тесты (новые, аддитивно):** `millioTests/UI/Services/Finances/FinanceAccountServiceMixedStoreTests.swift` — (a) smoke `loadAccountsPopulatesCardPickers` (CardCatalog+CardManager наполнены после loadAccounts, пикеры не пустые); (b) `mixedStoreLegacyArchiveRestoreLeavesCoreUntouched` (converted core-счёт + непроконвертированная легаси одновременно; легаси archive/restore зелёный, core-двойник не тронут); (c) `mixedStorePhysicalDeleteLeavesCoreUntouched` (легаси physical-delete не задевает core).
> **Гейт (протокол плашки, git stash clean-baseline):** build 0 ошибок; 3 новых теста зелёные (и в `-only-testing`, и в полном сьюте). Clean-baseline 13 failed vs with-diff 16 failed — 4 diff-only ID (`AccountsTotalsServiceTests/totalAtIsFastOnWarmCacheForLargeHistory`, `FinanceDynamicsCoreContributionTests`×2, `FinanceDynamicsViewModelTests/historical replay`) перепрогнаны ИЗОЛИРОВАННО → 0 падений = подтверждённые флаки (protected Dynamics + perf-timing кластер, не мои). Диф чисто аддитивный (1 untracked-файл, 0 правок продакшена/шаредов; `CardManager.shared` не загрязняет — все консюмеры re-setup перед чтением). **0 новых детерминированных красных.** Device-тест archive/restore/delete на core-счетах — PENDING (нет устройства).
- ~~Перевести `addAccountToGroup`/`removeAccountFromGroup`/`deleteAccountPermanently`/`restoreArchivedAccountToGroup`/`updateUnderlyingArchiveState` на `Account`/`AccountGroup`~~ → ОТЛОЖЕНО на 5c.7.3 (см. журнал: каскад + core-маршрут = мёртвый код/дубль).
- `CardCatalog`/`CardManager` populate — сохранить как residual (см. §1.3 граница), НЕ расширять типизацию.
- **[Fable-ревью]** `loadAccounts()` (`FinanceAccountService.swift:107-146`) — точка, где инициализируются `CardCatalog`/`CardManager`; порт на core не должен обрывать их наполнение (Fable-риск №7 — иначе пустые пикеры Cashflow/Cashback вне видимости тестов этого плана). Smoke-тест пикеров — обязательная часть гейта, не отложить на 5c.7.5.
- **[Fable-ревью, формулировка ужесточена]** Regression-тест на смешанном сторе (converted core-счёт + непроконвертированная legacy-запись одновременно) — **создать fixture, не проверять «если есть»**; без него легаси archive/restore-путь для непроконвертированных записей непроверяем.
- Гейт: archive/restore/delete device-тест на core-счетах; легаси archive/restore-путь на смешанном фикстур-сторе (см. выше) зелёный; смок-тест наполнения CardCatalog/CardManager (пикеры Cashflow/Cashback не пустые).

### 5c.7.3 — `FinanceViewModel` → core (L, самый большой кусок) — [~] ЧАСТИЧНО РЕАЛИЗОВАНО (защита+резидуал+mixed-store), ТИП-ФЛИП ОТЛОЖЕН атомарным блоком в 5c.7.4+5c.7.5 (ментор-находка, structural), Fable-верификация PENDING, НЕ закоммичено (2026-07-11, Александр)
> **Журнал (2026-07-11, Александр) — structural-находка: тип-флип 5c.7.3 НЕ реализуем как отдельно-гейтимая под-фаза.** Challenge Loop + grep-доказательства показали, что буквальный флип published-типов (`state.groups:[FinanceGroup]→[AccountGroup]`, `availableCards/Credits/Investments→[Account]`, return-типы `FinanceGroupService`) — **атомарный по построению** через `FinanceViewModel` + `FinanceDynamicsViewModel` (5c.7.4) + 7 View-файлов (5c.7.5) одновременно. Причины, каждой достаточно:
> 1. **Green-build gate физически запрещает частичный флип.** Published-`state.groups` читают (file:line): `FinanceDynamicsView:515/1324/2515/2865`, `FinanceDynamicsViewModel:548` (+ собств. `state.groups` 5c.7.4), `FinanceOverviewCardView:113/151`, `FinanceAddAccountView:107/267/296/470/478/486/725`, `StockBulkImportSheet:988/1564/1571`. `FinanceGroupService.visibleGroupsForList()/orderedAccounts(for:)` — `FinancesView:539/858`, `FinanceRows:204/236/346`, `FinanceGroupEditorView:64`. Все на `FinanceGroup.groupUniqueID(String)`/`.accounts([FinanceAccount])`; `AccountGroup` даёт `.id(UUID)`. Флип published-типа = немедленный компайл-фейл всех потребителей → «флип VM в 5c.7.3, Views в 5c.7.5» невозможен под гейтом «build 0 ошибок» на каждой под-фазе. Значит разбивка 5c.7.3/4/5 для тип-флипа нереализуема как отдельные гейты — это ОДИН атомарный коммит.
> 2. **Флип схлопывает dual-render → single-render.** Сейчас (`FinanceViewModel:892-1005`): `state.groups`=легаси-fetch, склеен по имени с `newCoreAccounts(matching:)`=core-fetch, `calculateGroupTotal`=legacyTotal+coreTotal. Флип убирает легаси-ветку рендера → непроконвертированный легаси-хвост нуждается в fallback-пути (Fable-риск №6), проверяемом на РЕАЛЬНОМ бэкапе+устройстве. В автономной сессии без владельца/девайса на самом-тестируемом модуле (`FinanceTotalsServiceFilterConsistencyTests`) — недопустимый риск (методология автономных сессий: стоп на риске, device PENDING ≠ форсировать).
> **Вывод (ментор):** тип-флип + снятие легаси-терма `calculateGroupTotal` (последнее = data-regression, пока легаси рендерится отдельно) переносятся ЕДИНЫМ атомарным блоком, склеенным с 5c.7.4 (FDVM) + View-сигнатурами 5c.7.5, делать с владельцем + device + реальный бэкап. Это НЕ 4-я отсрочка «чтобы не работать» — это структурный факт: под green-gate флип нельзя дробить.
> **Реализовано в этой сессии (safe, non-cascading, гейт зелёный):**
> 1. **Characterization-тесты ДО порта (Fable-риск №5), mandatory-first.** `millioTests/UI/Services/Finances/FinanceViewModelCharacterizationTests.swift` — 5 тестов с КОНКРЕТНЫМИ числами на ТЕКУЩЕМ легаси-коде: `legacyOnlyGroupTotal`=20000 (RUB, без конверсии), `mixedStoreGroupTotal`=17000 (легаси 10000 + core 7000, **mixed-store fixture инвариант 9**, `newCoreAccounts` находит core-хвост), `orderedAccountsByAmountDescending`=[large,medium,small], `visibleGroupsHidesEmptyUngrouped`, `newCoreAccountsExcludesArchived` (participates). Это контракт для будущего атомарного флипа: числа обязаны совпасть после порта. Так как флип отложен — тесты зелёные на легаси (порт-адаптация сигнатур будет вместе с флипом, числа те же).
> 2. **Унификация `resolveCoreGroup` (закрыт Fable-резидуал 5c.7.1).** `AccountsCoreAdditionBridge.swift:66-90` — добавлен name-overload `resolveAccountGroup(matchingName:colorHex:in:)` с ШИРОКИМ гардом `allKnownUngroupedNames`; старый `matching:`-overload делегирует ему (behavior-preserving). `FinanceViewModel.resolveCoreGroup(for:):1442` и `LegacyAccountsMigrator.resolveCoreGroup(name:):288` сведены на мост (раньше гардили Ungrouped только по `ungroupedName` текущей локали → кросс-локальный легаси-Ungrouped материализовался второй core-сущностью). +2 bridge-теста (`ByNameReturnsNilForEveryKnownUngroupedName`, `ByNameCreatesThenReusesByName`). Non-cascading: обе `resolveCoreGroup` приватны, сигнатуры не менялись.
> 3. **`physicallyDeleteLegacyAccount` core-split — НЕ реализован (Challenge Loop #3, dead-code).** Доказано: `FinanceAccountService.physicallyDeleteLegacyAccount:482-510` оперирует ТОЛЬКО легаси-моделями по легаси-ID (`Card/Credit/Investment` через `cardUniqueID` и т.д.); core-`Account` недостижим по построению, уже доказано тестом 5c.7.2 `mixedStorePhysicalDeleteLeavesCoreUntouched`. Маршрут в `AccountsCoreService.physicallyDelete(Account)` был бы мёртвой веткой (core-путь живёт отдельно `ArchivedAccountsView:133/144`) + `isConverted`-guard тут избыточен (UI-гейт `FinanceDynamicsView:1445` уже показывает только легаси-leftover). Data-critical delete-код в автономной сессии не трогаю без provable-дефекта — консистентно с dead-code-находкой 5c.7.2.
> **Гейт (протокол плашки, git-stash ID-сеты):** build 0 ошибок (0 новых warning — единственный warning `LegacyAccountsMigrator:55 registry=.shared` пре-существующий, untouched-строка). `FinanceViewModelTests` 72/72 зелёные (вместе с `FinanceTotalsServiceFilterConsistencyTests` — инвариант `Total(Groups)==Total(Accounts)` держит). 8-suite at-risk прогон (54 теста): все зелёные, кроме `GroupsMigrator/duplicateLegacyGroupNamesAreLoggedNotSilentlyDropped` — прогнан на CLEAN HEAD (git stash) с ИДЕНТИЧНЫМ падением (`isFavorite→false==true` GroupsMigratorTests:169) → пре-существующий fetch-order флак (плана :104), НЕ мой диф. ID-сет with-diff == clean-baseline = `{GroupsMigrator/duplicateLegacyGroupNames}` → **0 новых детерминированных красных**. Все прогоны `-parallel-testing-enabled NO` (parallel-clone даёт FBSOpenApplication launch-флаки). Device — PENDING. НЕ закоммичено (ждёт Fable-верификации).
- **[Перенесено из 5c.7.1, ментор-отсрочка]** Type-flip публичного API `FinanceGroupService` (`visibleGroupsForList()`, `orderedAccounts(for:)`) на `AccountGroup`/`[Account]` — часть этой фазы, не отдельная. Заодно унифицировать `LegacyAccountsMigrator.resolveCoreGroup:289-296` и `FinanceViewModel.resolveCoreGroup:1443-1453` на мост `AccountsCoreAdditionBridge.resolveAccountGroup` (закрывает Fable-резидуал 5c.7.1 — кросс-локальный Ungrouped мимо guard'а).
- **[Перенесено из 5c.7.2, ментор-отсрочка]** Type-flip публичного API `FinanceAccountService` (`addAccountToGroup`/`removeAccountFromGroup`/`deleteAccountPermanently`/`restoreArchivedAccountToGroup`/`updateUnderlyingArchiveState`) + экшенов VM (`FinanceViewModel:169-174`). При этом НЕ маршрутизировать эти методы в `AccountsCoreService.archive/restore/physicallyDelete` — core-путь уже живёт отдельно (`AccountDetailView:846`, `ArchivedAccountsView:133/144`); `FinanceAccountService` остаётся легаси-fallback для немигрированных leftover (инвариант 5). Развести `physicallyDeleteLegacyAccount`-экшен от core-`physicallyDelete` явно.
- `state.groups`→`[AccountGroup]`, `availableCards/Credits/Investments`→единый `[Account]`, кэши по `Account.id`.
- `editGroup`/`removeAccountFromGroup`/`showAccountDynamics` — параметры на `Account`/`AccountGroup`.
- `physicallyDeleteLegacyAccount`-экшен — переименовать/развести на core-путь (`physicallyDelete` из 5c.7.2) + legacy-fallback только если легаси-запись реально есть.
- `calculateGroupTotal` — снять легаси-терм (Ф2c-блокер (в), теперь безопасно — VM уже core-typed).
- **[Fable-ревью] Characterization-тесты ДО порта.** Гейт «FinanceViewModelTests переписанные под core зелёные» самоподтверждается — переписанный тест не ловит регресс поведения, которое сам же переопределяет (Fable-риск №5, вероятность высокая). Перед портом зафиксировать текущее поведение totals/ordering/фильтров отдельными characterization-тестами на ТЕКУЩЕМ легаси-коде, затем гонять их же (адаптированные под новую сигнатуру, но с теми же ожидаемыми числами) после порта.
- Mixed-store fixture (инвариант 9, §2.1) — обязательная часть regression-сьюта этой под-фазы, не только 5c.7.4.
- Гейт: characterization-тесты (сняты ДО порта) зелёные после порта; `FinanceViewModelTests` полностью зелёные; 0 новых красных в остальном сьюте.

### 5c.7.4+5c.7.5 (ОБЪЕДИНЕНО, 2026-07-11) — атомарный type-flip FinanceViewModel+FinanceDynamicsViewModel+Views (XL, самый крупный гейт плана) — [x] РЕАЛИЗОВАНО, Fable-подтверждено (терминальный гейт + повторная точечная проверка), закоммичено `871dfc0` (2026-07-11, НЕ запушено)

> **Ментор-стоп ДО кода (Александр, 2026-07-11), затем возобновление.** Александр провёл research и отказался начинать правку без явного решения — реальный охват флипа оказался существенно больше, чем описано в плане, у него нет безопасного промежуточного билдящегося состояния. Владелец сначала подтвердил «отдельная сессия», затем уточнил риск-профиль (Millio на этом этапе — фактически один реальный пользователь, сам владелец, данные восстановимы из бэкапа) и явно снял требование обязательной отдельной сессии/device-хард-гейта — **дисциплина build+xcresulttool+Fable-верификации остаётся полностью**, продолжаем в этой же сессии.
>
> **Находки research'а (важны для следующей сессии, не терять):**
> 1. **Формулировка плана «снять легаси-терм» — ОШИБОЧНА, зафиксировать design fix.** `calculateGroupTotal` = `legacyTotal + coreTotal` (`FinanceViewModel.swift:892-898`). Простое удаление легаси-терма даёт `mixedStoreGroupTotal`: 17000→7000 — деньги юзера пропадают из тотала. **Правильный дизайн: инверсия primary/fallback** — core становится primary-источником, легаси остаётся fallback-по-имени для непроконвертированных записей, а не выбрасывается.
> 2. **Хорошая новость, снижает реальный риск:** главный агрегат «Общий баланс» в шапке (`FinanceTotalsService.swift:69-83`) УЖЕ core-only (легаси-цикл снят в 6b Ф2). Dual-render остался только в per-group display/строках — самый заметный юзеру расчёт флип не трогает.
> 3. **Скоуп шире плана.** `state.groups`/`availableCards` питают не только Views, а провайдеры сервисов: `FinanceTotalsService`/`AccountBalanceSnapshotService`/`FinanceGroupService` (`FinanceViewModel.swift:255/272/300`; `FinanceTotalsService.swift:27,44,258-260` — `cardByID/creditByID/investmentByID`). Флип = переписывание account-info резолвинга (`getAccountInfo:906`, `getAccountInfoForDynamics`, `cardsCache`) с легаси-словарей на core.
> 4. **Кросс-скоуп связь, не была в плане:** `FDVM.getAccountsForCalculation()→[FinanceAccount]` (`:908/:980`) и `calculateBalanceAtDate(accounts:)` (`:1979`) потребляются **Cashflow** (`CashflowViewModel+Categories.swift:364-383`) — вне скоупа 5c.7 (Cashback-миграция — отдельный эскалированный скоуп). Флип FDVM обязан СОХРАНИТЬ эти сигнатуры легаси-типизированными — не трогать транзитивно.
> 5. **Оценка реального объёма:** ~150-250 сайтов в ~16 файлах (2 God-VM 1867+2915 строк, `FinanceDynamicsView` 3202 строки, + сервисы + Views) — не помещается в один атомарный гейт автономной сессии без device.
>
> **Рекомендация для следующей сессии:** отдельная сессия, полный бюджет, реализовать по исправленному дизайну (п.1) с сохранением Cashflow-сигнатур (п.4). Research выше = карта сайтов, грайнд должен быть быстрее благодаря этому.

> **Второй стоп (Александр, 2026-07-11, после риск-калибровки владельца) — не переигровка риска, уточнение метода.** Владелец снял хард-гейт «device+отдельная сессия», Александр продолжил в ЭТОЙ ЖЕ сессии и провёл ещё более глубокий research (полное чтение `Account.swift`/`AccountGroup.swift`/`AccountBalanceEngine.swift`/`AccountsCoreService.swift`/`FinanceGroupService.swift`/`AccountsCoreAdditionBridge.swift`/`FinanceRows.swift`). Нашёл: реальный флип — не механическое переименование типов, а переписывание финансовой семантики:
> 1. `updateAccountAmount` (`FinanceViewModel.swift:1528-1796`, 270 строк) создаёt `CashflowTransaction`-аудит с разными знаковыми конвенциями per-тип счёта; core-эквивалент `AccountsCoreService.adjustBalance` (`:109`) уже делает это через `AccountEvent`-журнал — 270 строк потенциально схлопываются в ~15, но это нужно доказать тестом, не предположить.
> 2. Ungrouped-виртуализация не решена архитектурно: `AccountGroup` не может представлять «без группы» (канон — `account.group == nil`), значит `visibleGroupsForList()→[AccountGroup]` физически не включает Ungrouped как сущность — нужен sentinel-объект или отдельный UI-путь, влияет на каждый View со списком групп.
> 3. `FinanceRows.swift` (828 строк, прочитан целиком) — investment/credit-подзаголовки завязаны на легаси-геттеры (`getInvestmentPositionSubtitle` и т.д.), порт реален (core уже хранит `creditLimit`), но это переписывание логики, не rename.
> 4. `convertAccountToCore`/`unconvertAccountFromCore` (Track C, ~70 строк) — роль после флипа неочевидна, в плане не описана.
> **Вывод: атомарный blind full-send флип ~250 правок с ЕДИНСТВЕННОЙ точкой проверки в конце — риск тихой ошибки в знаке долга или схлопывании Ungrouped, которую тест может не поймать с первого раза.** Дерево не тронуто (0 diff на `e49ef3a`).
>
> **Рекомендация Александра (принята) — expand-contract вместо blind full-send.** Завести `state.coreGroups: [AccountGroup]`/`state.coreAccounts: [Account]` РЯДОМ со старыми полями (design D1-D6 уже проработан: core-primary+legacy-fallback-по-имени, sentinel-Ungrouped, `adjustBalance`-реюз для quick-edit), мигрировать потребителей файл-за-файлом с build+тест-проверкой на КАЖДОМ файле, снести старые поля последним шагом. Больше кода временно, но каждый шаг верифицируем отдельным Fable-гейтом — согласуется с паттерном всего дня (5c.7.0-5c.7.3), а не разовый огромный блок.
>
> **Решение владельца (2026-07-11):** передать expand-contract-реализацию в НОВЫЙ чат (эта сессия слишком длинная для продолжения). Следующая сессия начинает с: (а) прочитать этот план целиком + `progress/2026-07-11-day-6b-phase-5c7-finances-replatform.md`, (б) реализовать `state.coreGroups`/`state.coreAccounts` expand-шаг №1 (добавление полей, ничего не удалять), (в) мигрировать потребителей по одному файлу за раз, каждый — свой Fable-подтверждённый гейт и коммит, (г) contract-шаг (снос легаси-полей) — только когда ВСЕ потребители мигрированы и это доказано grep'ом.

> **Журнал expand-фазы (2026-07-11 ночь, новая сессия) — ВЫПОЛНЕНО, 9 коммитов `a5169a7→f88abd2` (локально).** Конвейер: Александр (opus) кодит → независимый Fable-верификатор адверсариально проверяет → коммит только после CONFIRMED. Три отклонения гейтов с реальными дефектами, все закрыты фиксами в корне:
> 1. **Expand-шаг** (`a5169a7`): `state.coreGroups`/`state.coreAccounts` рядом с легаси, populate через participates-семантику, канон Ungrouped=`group==nil`.
> 2. **№1 FinanceRows** (`2736945`): живой fetch → снапшот. Fable-отклонение №1: refresh-gap — снапшот обновлялся только на CREATE; archive/restore/physicallyDelete/QuickSetup давали ghost-строки (у physicallyDelete — висячая ссылка на удалённый @Model). Корневой фикс: `loadCoreEntities()` в `subscribeToFinanceEvents`, публикация `investmentsUpdated` из `ArchivedAccountsView`/`QuickSetupApplier`; adjustBalance живой через @Model (доказано тестом). +7 тестов membership.
> 3. **№2 FinanceGroupEditorView** (`f20205f`): редактор группы впервые показывает core-счета; единая точка `coreAccountsSnapshot(matching:)` (FinanceRows переведён на неё же). Fable-отклонение №2: `.onDisappear` на label NavigationLink срабатывает при push ВПЕРЁД, не на pop-back — перевешен на destination (комментарий-предупреждение в коде).
> 4. **№3 FinancesView** (`6855653`): `isGroupEmpty` — живой FetchDescriptor на каждую группу/рендер → снапшот. №4 StockBulkImportSheet — no-op (5 хитов flip-blocked, refresh уже корректен). 
> 5. **№5 OverviewCard** (`dd85c40`): ledger на снапшот + core-aware `reloadToken` (id/name/events.count/archivedAt/group.id + coreGroups; rename-гэп закрыт по Fable-находке).
> 6. **Ungrouped-фикс** (`6c31bd5`): пустая легаси-Ungrouped скрывалась только по легаси-критерию → при core-only ungrouped бакет исчезал из карточки/строк (разрыв суммы видимых строк). `loadCoreEntities()` переставлен в начало `loadGroups`, фильтр `hasLegacy || hasCoreUngrouped`; анти-дубль тест (ровно один Ungrouped-бакет). Вероятно закрывает хвост «дубль Ungrouped на Счетах» из 6b — нужна визуальная проверка владельца.
> 7. **№6 FinanceAddAccountView — no-op** (18 хитов = единый легаси-контур group-selection, каскад доказан). 🔴 **Найден живой обход паволла:** `currentFinanceProductCount` (:1296-1298) считает только легаси `available*`, а файл создаёт core-счета → free-юзер обходит `freeFinanceProductLimit=10`. Фикс меняет gating → ТРЕБУЕТ решения владельца (не тронуто).
> 8. **№7 providers — no-op**: `calculateGroupTotal` уже expand-комбинатор; `AccountBalanceSnapshotService` — sparkline-gap для core-строк = feature gap, не regression (решение — `AccountDailySnapshot`-система, не дубль писателя); group-сервис core-aware с 5c.7.1.
> 9. **Gate A** (`0c23e0b`): `updateCurrencyBreakdown` видел только легаси — все core-счета давали 0 в разбивке по валютам, twin-пара = полная потеря (0). Fable-отклонение №3: дедуп-слой Layer 2 не был доказан; мутационная проверка вскрыла, что он НЕДОСТИЖИМ (безусловный гард `archivedAt==nil` в `signedValue`) — удалён как dead code. TDD: mixed-store 10000→17000, twin 0→100000 (и при showArchivedAccounts=true). Известное ограничение: `coreAccountsForDynamics()` не уважает `selectedAccountIDs` (унаследовано, комментарий в коде).
> 10. **Gate B** (`f88abd2`): characterization-замок `updateAccountAmount` (11 тестов) + core-parity (2). Ключевое для флипа: legacy инвертирует знак долга вручную при создании аудита; core `adjustBalance` пишет сырую дельту (интерпретация в `AccountBalanceEngine` signMap), `.loan/.debt` ждут newValue в net-worth-конвенции; market-инвестиции: legacy правит QUANTITY, core — отдельный путь buy/sell. Полная таблица — в шапке `FinanceViewModelUpdateAccountAmountCharacterizationTests.swift`.
>
> **Каталог flip-blocked для contract-шага (из research №8):** FVM published (`state.groups`, `availableCards/Credits/Investments`, `cardByID/...`) ← FDVM:548,595-596, OverviewCard, FinanceAddAccountView×7, StockBulkImportSheet, FinancesView, FinanceRows, GroupEditor; FDVM `FetchDescriptor<Card/Credit/Investment>`:523-529,576-583 привязаны к `cardsCache/...` (10+ потребителей, включая `FinanceNetWorthSignedAmount.signedValue`); FDV `state.groups`×4 (:515,1324,2515,2865); граница Cashflow: `getAccountsForCalculation`(:908,:980)/`calculateBalanceAtDate`(:1979) — сигнатуры НЕ трогать. Семантические узлы: `updateAccountAmount`→`adjustBalance` (карта Gate B), Ungrouped-сентинел для `visibleGroupsForList()→[AccountGroup]`, `FinanceRows`-подзаголовки, `convertAccountToCore`/`unconvert` — решение владельца 2026-07-12: ОСТАВИТЬ до его ручной очистки пары легаси-счетов, при флипе не сносить.
>
> **Слияние по Fable-находке из 5c.7.3.** Разбивка 5c.7.4/5c.7.5 на раздельно гейтуемые под-фазы физически нереализуема под правилом «build 0 ошибок на каждом гейте» — published-типы `state.groups`/`availableCards`/`FinanceGroupService`-return читаются одновременно из `FinanceDynamicsViewModel`, `FinanceDynamicsView`, `FinanceOverviewCardView`, `FinanceAddAccountView`, `StockBulkImportSheet`, `FinancesView`, `FinanceRows`, `FinanceGroupEditorView` — смена типа компайл-фейлит их все разом (полное доказательство file:line — журнал 5c.7.3 выше). Fable независимо подтвердил: инкрементальный shim невозможен без второго источника правды (инвариант 2) или фантомных SwiftData-инстансов. Владелец подтвердил (2026-07-11, на связи): продолжать сегодня одним гейтом.

**Скоуп (объединяет исходные 5c.7.4 + 5c.7.5 + перенесённые пункты из 5c.7.1/5c.7.2/5c.7.3 — см. их журналы выше):**
- `state.groups`→`[AccountGroup]`, `availableCards/Credits/Investments`→единый `[Account]`, кэши по `Account.id` (`FinanceViewModel`).
- `editGroup`/`removeAccountFromGroup`/`showAccountDynamics` — параметры на `Account`/`AccountGroup`.
- `calculateGroupTotal` — снять легаси-терм (безопасно теперь, VM core-typed) — схлопывает dual-render легаси+core в single-render.
- `physicallyDeleteLegacyAccount`-экшен — развести на core-путь (`AccountsCoreService.physicallyDelete`) + legacy-fallback только если легаси-запись реально есть (не мёртвый код — теперь VM core-typed, маршрутизация реальна).
- Type-flip `FinanceGroupService`/`FinanceAccountService` публичного API на call sites VM/Views (уже унифицированные в 5c.7.1/5c.7.2/5c.7.3 внутренности переиспользовать как есть).
- `FinanceDynamicsViewModel`: `FetchDescriptor<Card/Credit/Investment>()` `:523-529` → `FetchDescriptor<Account>`; per-account chart-modes (byAccounts/singleAccount) — порт на `Account`, `aggregateGroupRows`/`signedAccountValue`-паттерн без изменений семантики.
- Views: `FinancesView`/`FinanceRows`/`FinanceOverviewCardView`/`FinanceAddAccountView`/`FinanceGroupEditorView`/`FinanceQuickEditAccountView`/`ArchivedAccountsView`/`FinanceDynamicsView`/`StockBulkImportSheet` — сигнатуры на core-типы.
- Декаплинг `InlineCreateForms` от `CardViewModel`/`CreditViewModel`/`InvestmentViewModel`.
- Полноценный rich-edit UI в `AccountDetailView` (переиспользовать теперь core-типизированную `FinanceAddAccountView` в режиме prefill-from-core вместо минимальной формы 5c.7.0).
- Mixed-store fixture (инвариант 9) — `FetchDescriptor<Account>`-миграция не должна тихо терять непроконвертированный legacy-хвост из графика/breakdown (Fable-риск №6); characterization-тесты из 5c.7.3 обязаны пройти с ТЕМИ ЖЕ числами после адаптации сигнатур (Fable-риск №5).

> **Журнал contract-шага (2026-07-11, новая сессия, продолжение) — ВЫПОЛНЕНО, коммит `871dfc0` (локально, НЕ запушено).** Атомарный флип за один непрерывный проход (промежуточные состояния не билдились, как и предсказано — компайл-фейл всех потребителей одновременно при смене published-типа): `FinanceState`/`FinanceAction`/`FinanceViewModel`/`FinanceDynamicsViewModel`/`FinanceGroupService`/7 Views на `AccountGroup`/`Account`.
> 1. **Design fix `calculateGroupTotal`** (журнал первого стопа, п.1) реализован: core primary (`orderedAccounts(for:)` + `accountsTotalsService.total`) + легаси fallback-по-имени (`fetchLegacyGroup`/`totalsService.calculateGroupTotal`) — mixed-store тотал 17000 (core 7000 + легаси 10000), не 7000.
> 2. **`updateAccountAmount`/`updateCreditCardQuickFields`** флипнуты на `AccountsCoreService.adjustBalance` по Gate B parity-таблице (net-worth-конвенция, без ручной инверсии знака). Легаси-тела (270 строк) НЕ удалены — извлечены как `updateLegacyAccountAmount`/`updateLegacyCreditCardQuickFields` (реальный потребитель — inline-edit одного легаси-счёта в `FinanceDynamicsView`, доказано текстом, не мёртвый код).
> 3. **`addAccountToGroup`/`removeAccountFromGroup`/`deleteAccountPermanently`/`restoreArchivedAccountToGroup`** — core-путь (прямая мутация `.group`/`AccountsCoreService.physicallyDelete`/`restoreAccount`) + явные легаси-wrapper'ы (`addLegacyAccountToGroup` и т.д.) для fallback-хвоста (invariant 5).
> 4. **`.showAccountDynamics`/`state.selectedAccountForDynamics` — ментор-находка, ОСТАВЛЕНЫ легаси-типизированными** вопреки исходной формулировке скоупа: реальный потребитель (`FinanceDynamicsView.initialAccount`) — rich single-account screen с convert-to-core/purge-легаси/inline-credit-card-edit, неотделимо легаси-ориентирован; core-счета уже имеют СВОЙ путь (`AccountDetailView`, richer UI, `AccountAdjustBalanceSheet`) — дублировать было бы избыточно (Challenge Loop #3, тот же паттерн, что 5c.7.2/5c.7.3 ментор-отсрочки).
> 5. **Ungrouped-регресс, пойман mixed-store тестом.** `ungroupedAccounts()` был подключен только к `FinanceOverviewCardView` (дашборд), но не к `FinancesView` (экран «Счета») — core-счета без группы стали бы невидимы на главном списке. Найдено ДО коммита через `FinanceViewModelCoreEntitiesTests` (2 теста упали на реальной архитектурной дыре, не флаке — перепроверено method-level `-only-testing` false-positive паттерном, см. п.7) — добавлена отдельная Ungrouped-секция в `FinancesView.groupsListView`.
> 6. **Тестовые Schema-пробелы (3 файла).** `FinanceViewModelTests`/`BackupRestoreIntegrityTests`/(и struct-локальные `Schema([...])` в целом) не включали `Account`/`AccountGroup`/`AccountEvent`/`AccountDailySnapshot` — core-фикстуры в тестах молча не сохранялись/не читались (тип не зарегистрирован в SwiftData Schema, НЕ ошибка `insert`/`save`). Добавлены явно, как уже было сделано в `FinanceViewModelCharacterizationTests`/`AppMigrationPlan.makeInMemoryContainer()`.
> 7. **Ловушка `-only-testing` для Swift Testing.** Method-level идентификатор (`Suite/testMethod`) в этой Xcode-версии не резолвится и МОЛЧА запускает 0 тестов (`Executed 0 tests` → `EXIT=0`, ложный «зелёный»). Использовать только suite-level `-only-testing:millioTests/SuiteName` — метод внутри проверять через полный non-quiet вывод (`grep "✘.*recorded an issue"`), не через повторный `-only-testing` на сам метод.
> 8. **git-stash baseline-протокол.** Полный `millioTests` дал ID-сет failed идентичный clean-baseline (`1a9f51f`, `git stash`), минус 2 находки этого гейта, которые были красными на моём дифе, но НЕ на baseline — обе реальные регрессии архитектуры (не флаки), обе закрыты ДО коммита: (а) `BackupRestoreIntegrityTests.testFinanceViewModelShowsDataAfterRestoreRoundTrip` — проверял легаси-имя в `state.groups` (core), плюс тот же Schema-пробел (п.6); (б) `GroupsMigratorTests` — подтверждённый пре-существующий флак (как и задокументировано в плане ранее).
> **Point-фиксы после финального Fable-ревью (владелец 2026-07-12), тот же коммит:**
> 1. **`FinanceGroupService.deleteGroup` — возвращена архивация** (`AccountsCoreService.archiveAccount` перед `.group = nil`). Контрактный флип случайно заменил легаси-поведение «удаление группы архивирует счета» на голый `.nullify` — отменено как регресс, не новая фича.
> 2. **Снесён мёртвый код**: `FinanceQuickEditAccountView.swift` (файл) + `state.showQuickEditAccountSheet`/`quickEditAccount` + `.showQuickEditAccountSheet`/`.hideQuickEditAccountSheet` actions — grep подтвердил 0 продакшн-вызовов после флипа (единственный caller, sheet в `FinancesSheets.swift`, никогда не активировался — ничто не выставляло `showQuickEditAccountSheet = true`). Редактирование остаётся доступно через `AccountDetailView`/`AccountAdjustBalanceSheet`.
> **PENDING (не блокеры, зафиксировано, не терять):**
> - Интерактивный UI-walkthrough в симуляторе (create→edit→archive→restore→delete на 3 типах счетов) НЕ выполнен — macOS Accessibility permissions заблокированы для automation в этой среде (`osascript`/System Events → `-1743`). Автоматический smoke-launch (чистый simulator erase → запуск → онбординг без краша) выполнен и зелёный. Нужна ручная проверка владельцем перед пушем/релизом.
> - 3 adversarial-находки финального Fable, не проверялись глубоко (не регрессии, не блокеры): (а) `try?` в `archiveAccount` при `deleteGroup` (`FinanceGroupService.swift:68`) молча глотает ошибку сохранения — нет пользовательского фидбека при сбое; (б) повторное удаление уже архивированной группы/счёта перезапишет `archivedAt` на текущий `now` (не идемпотентно); (в) не подтверждено визуально, что `.showAccountDynamics` действительно открывает rich-edit (не read-only sheet) для легаси-хвоста — логика заявлена, но не проверена глазами.
> **Тесты (после гейта, суммарно по затронутым сьютам):** protected Dynamics-кластер (6 файлов) зелёный; `FinanceTotalsServiceFilterConsistencyTests` зелёный; `FinanceViewModelCharacterizationTests`/`FinanceViewModelUpdateAccountAmountCharacterizationTests` (Gate B) зелёные, числа сохранены; `FinanceViewModelCoreEntitiesTests` (mixed-store) зелёный; `FinanceGroupServiceCRUDTests`/`FinanceGroupServiceAccountsCoreTests` зелёные (переписаны на новую CRUD-семантику); `FinanceViewModelTests` (общий, 69 тестов) зелёный; `BackupRestoreIntegrityTests`/`FinanceLifecycleIntegrationTests`/`FinanceAccountServiceMixedStoreTests` зелёные. Полный `millioTests` — 0 новых красных относительно `1a9f51f` (git-stash протокол, п.8 выше).

**Гейт (объединённый, самый строгий во всём плане):**
1. build 0 ошибок.
2. **Обязательный полный прогон 6-файлового protected-кластера Dynamics-тестов** (~3347 строк) — 0 новых красных (протокол git-stash ID-сетов из плашки).
3. `FinanceTotalsServiceFilterConsistencyTests` зелёный (инвариант `Total(Groups)==Total(Accounts)`).
4. `FinanceViewModelCharacterizationTests` (5c.7.3) — те же числа после порта.
5. Mixed-store fixture зелёный (и для FVM, и для FDVM).
6. UI-проверка (симулятор — device не хард-гейт, см. риск-калибровку) полного flow «создать → отредактировать (rich-edit) → архивировать → восстановить → удалить» на 3 типах счетов.
7. Fable-независимая верификация ОБЯЗАТЕЛЬНА перед коммитом (как и все гейты дня) — здесь особенно строго ввиду рискованности.

### 5c.7.6 — Редизайн «Счета» (Ф6 proper, M) — слито сюда по решению плана-родителя
- Stacked-полоса активы/обязательства с нетто; секции «Активы»/«Обязательства» с подытогами; `AccountGroup.customIconName` (паттерн `AccountIconPickerSheet`/`FinanceOverviewLedgerPresentation`, additive-поле, без V6).
- Шапка: вторичная валюта чипом; БЕЗ чипа прироста/sparkline (решение владельца 2026-07-08).
- Побочные полировки, естественно попадающие в скоуп (не форсировать лишнее): дубль Ungrouped — уже закрыт в 5c.7.1 структурно, здесь только визуальная проверка; «Скрытые»/пустые состояния групп.
- Гейт: визуальный review + device stress-test полного экрана «Счета» на реальном бэкапе владельца.

### 5c.7.7 — Финальный regression-гейт под-проекта
- Полный `millioTests` через `xcrun xcresulttool`: 0 новых красных vs 1745/14.
- Grep-подтверждение: `FinanceViewModel`/`FinanceDynamicsViewModel`/`FinanceAccountService`/`FinanceGroupService` — легаси-ссылки сокращены до fallback-путей для непроконвертированных записей (явно перечислить оставшиеся, если есть).
- Обновить пред-гейт-грепу `plans/2026-07-07__legacy-accounts-purge-path-b.md` (стр.459) — переклассифицировать (c)-Finances-кластер, проверить, снят ли блокер 5c.6/5c.11 (скорее нет полностью — 5c.8/5c.9/5c.10 остаются, но Finances-доминанта должна исчезнуть).
- Device stress-test на реальном бэкапе владельца + явное «да» перед мержем в develop.

---

## 4. Риски (топ-3 + Fable-ревью 2026-07-11)
1. **`FinanceTotalsServiceFilterConsistencyTests`/protected Dynamics-кластер (~3347 строк)** — самый большой риск регресса; каждая под-фаза 5c.7.1/3/4 обязана гонять его целиком, не точечно.
2. **`updateAccount` (5c.7.0) — новый event-sourced API, риск рассинхрона имени/группы с существующими `adjustBalance`/`archiveAccount`** — писать с явным regression-guard теста на дубли (по аналогии с `AllPresetsOnNewCoreTests`), не форсировать без него. Валюта — вне скоупа v1 (§2.1 инвариант 8).
3. **`CardCatalog`/`CardManager`-граница (§1.3)** — если 5c.7.2 случайно оборвёт populate-логику, ломается Cashflow/Cashback (вне видимости тестов этого плана) — явно тестировать смежные экраны при device-стресс-тесте 5c.7.2/5c.7.5.

**Fable-ревью, 10 причин провала (полный стресс-тест) — учтены точечно по под-фазам выше, сводка:**
1. Смена валюты в updateAccount переинтерпретирует историю → средняя вероятность → запрещена в v1 (инвариант 8).
2. Мутация группы мимо кэшей рвёт Total(Groups)==Total(Accounts) → средняя → invalidateSnapshotCache + FilterConsistency-тест сразу после updateAccount (5c.7.0).
3. Rich-edit UI на легаси-типизированной форме реинкарнирует Ф6a-дубликат-баг → средняя → UI перенесён в 5c.7.5 (после перетипизации формы).
4. Ungrouped как отдельная AccountGroup-сущность вместо канонизации `group==nil` → высокая при исходном варианте → скорректировано в 5c.7.1.
5. Самоподтверждающийся гейт «переписанные тесты зелёные» → высокая → characterization-тесты до порта (5c.7.3).
6. `FetchDescriptor<Account>` теряет legacy-хвост из графика на смешанном сторе → средняя → mixed-store fixture (5c.7.3/5c.7.4, инвариант 9).
7. Порт `loadAccounts()` обрывает CardCatalog/CardManager → пустые пикеры Cashflow/Cashback → средняя → smoke-тест пикеров в гейте 5c.7.2 (не отложен на 5c.7.5).
8. Неверный признак конвертации в physicallyDelete удаляет не тот счёт → низкая/средняя → тест на converted+unconverted пару, device PENDING.
9. Backup/restore roundtrip ломается на новых полях/событиях → средняя → roundtrip-тест в 5c.7.0 и 5c.7.6 (инвариант 7).
10. «0 новых красных» маскирует миграцию baseline-красного теста на другую причину падения → средняя → сравнивать список ID упавших тестов, не только счётчик (см. заголовок плана).

## 5. Что НЕ входит в этот план (остаётся в родительском плане)
- 5c.8 (Cashflow legacy-name-резолв), 5c.9 (MarketData `Investment`→core), 5c.10 (`ScreenshotDataSeeder`), 5c.11 (собственно снос @Model+V6+version-gate) — отдельные под-фазы плана-родителя, стартуют ПОСЛЕ этого плана.
- Миграция Cashback на AccountsCore — отдельный эскалированный скоуп (Ф3-решение владельца, вариант Б: `Card.swift` живёт дольше как spec-исключение).

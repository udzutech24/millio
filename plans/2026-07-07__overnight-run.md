# Ночной автономный прогон 2026-07-07 → 2026-07-08

**Статус:** ПОДТВЕРЖДЁН ВЛАДЕЛЬЦЕМ 2026-07-07 (Ф3 — да с условиями; 6b — план + начать реализацию; запуск — владелец, новым чатом). Ждёт запуска + caffeinate.
**Цель:** к утру — собранное приложение со всеми фазами редизайна Cashflow add-flow (Ф1–Ф4 + хвост Ф0) и готовым планом 6b Путь B. Всё проверено тестами и сборкой, отчёт готов.
**Базовый план фаз:** [`2026-07-05__cashflow-add-transaction-redesign.md`](2026-07-05__cashflow-add-transaction-redesign.md) — верифицирован, все решения владельца приняты, мокап-эталон рядом (`*.mockup.html`). Верстать в токенах проекта (AppTypography/AppSpacing/AppAnimation).
**Гейт Ф3:** пройден 2026-07-07 (стресс-тест, см. §4) — реализация разрешена при 5 условиях (§4).

## 1. Скоуп ночи (по порядку исполнения)

| # | Задача | План/секция | Исполнитель | Модель | Ветка | Мерж в develop ночью |
|---|--------|-------------|-------------|--------|-------|----------------------|
| 1 | **Ф1** — BudgetHeroCard наружу | базовый план, Фаза 1 | Александр | sonnet | `feature/cashflow-phase1-budget-card` | ✅ да, после гейта |
| 2 | **Ф2** — избранные категории (cap 8) | Фаза 2 | Александр | sonnet | `feature/cashflow-phase2-pinned-categories` | ✅ да |
| 3 | **Ф4** — breakdown топ-3 | Фаза 4 | Александр | sonnet | та же, что Ф2 (обе мелкие) | ✅ да |
| 4 | **Хвост Ф0** — секция «Предстоящие» на главном экране | Фаза 0, Шаг 6 | Александр | sonnet | `feature/cashflow-phase0-upcoming-section` | ✅ да |
| 5 | **Ф3** — единый экран Расход\|Доход\|Перевод (самое тяжёлое — последним, по ночному регламенту) | Фаза 3 + условия §4 | Александр | **opus** | `feature/cashflow-phase3-unified-sheet` | ❌ НЕТ — ждёт ручной проверки владельца |
| 6 | **План 6b Путь B** (документ; можно параллельно с Ф1–Ф4 — только чтение) | новый `plans/2026-07-07__legacy-accounts-purge-path-b.md` | Максим/Plan | opus | — | — |
| 7 | **6b Путь B, фаза 1 реализации** (одобрено владельцем 2026-07-07; строго ПОСЛЕ Ф3, не параллельно — общие Cashflow-мосты) | план из №6 | Александр | **opus** | `feature/legacy-accounts-purge` | ❌ НЕТ — план никем не ревьюился, ждёт утренней проверки |
| 8 | Финальная сборка + отчёт | §6–7 | координатор | — | — | — |

**Вне скоупа ночи (жёстко):** фазы 6b дальше первой; декомпозиция FinanceDynamics; кредиты→Cashflow; монетизация; любой push в origin; TestFlight/деплой; правки management-экранов (Планировщик, План доходов/расходов, Массовый импорт — сохраняются полностью, см. §5 базового плана).

**Условия 6b-реализации ночью:** стресс-тест развилки пройден 2026-07-06 (вводная 1–2 юзера, снос признан безопасным, Путь B одобрен владельцем); тем не менее план из №6 не ревьюился → фаза 1 идёт в `feature/legacy-accounts-purge` от свежего develop, БЕЗ мержа; если план №6 к моменту старта выявит блокер (несносимая сцепка, невозможность миграции) — реализацию НЕ начинать, зафиксировать блокер в отчёт. Если после Ф3 остаётся <25% бюджета окна — №7 пропустить в пользу качественного отчёта.

## 2а. Бюджет токенов, лимиты, контекст (требование владельца 2026-07-07)

**Иерархия моделей — «Fable как руководитель, лёгкие где можно»:**
- **Координатор ночи — самая сильная доступная модель** (Fable, пока доступен; иначе opus). Координатор НЕ пишет код: только ведёт фазы, принимает/бракует результаты гейтов, решает спорное, пишет отчёт.
- **Код фаз — гибрид (решение владельца 2026-07-07):** лёгкие фазы (Ф1, Ф2, Ф4, хвост Ф0 — расписаны в плане пошагово) — Александр на **sonnet**; тяжёлые (**Ф3** и **6b фаза 1**) — Александр на **opus**. Ревью результата каждой фазы делает координатор по diff'у + acceptance criteria (не перечитывая всю кодбазу).
- **Рутина — haiku:** baseline-прогон тестов, поиск/Explore, форматирование отчёта, актуализация статусов в планах.
- **План 6b — opus** (архитектурная инвентаризация).

**Экономия токенов:**
- Каждая фаза = свежий суб-агент с узким брифом (цель + пути файлов + AC + формат ответа ≤200 слов). Никаких full-Read крупных файлов; grep → узкий Read (правила CLAUDE.md обязательны и ночью).
- Координатор держит контекст ≤50%: после каждой фазы — только вердикт гейта и статус, детали остаются в планах/progress. При приближении к 50% — handoff в `progress/2026-07-08-overnight-handoff.md` и продолжение со сжатым контекстом.
- Вывод xcodebuild/тестов — фильтровать (последние строки/только failures), но НЕ глушить grep'ом сборочные ошибки (память 2026-07-05).
- `/bulletproof` при необходимости (сложная под-задача внутри Ф3/6b) — ТОЛЬКО через суб-агента, не в главном окне (хук блокирует; память feedback-bulletproof-via-subagent).

**Поведение при исчерпании лимитов (обязательное):**
1. Ловим сигнал (rate-limit / отказ API / резкое сокращение окна) → немедленно: закоммитить текущее состояние ветки, дописать handoff (что сделано / что осталось / следующая команда), обновить статусы в планах.
2. **Не бросать прогон** — приостановиться и возобновить, когда лимиты восстановятся (окно 5-часовое): координатор ждёт и продолжает с handoff'а. Если сессия умерла — владелец продолжает одной командой: «Продолжи ночной прогон по плану plans/2026-07-07__overnight-run.md с handoff'а».
   **Второй аккаунт (владелец, 2026-07-07):** при исчерпании лимита основного аккаунта прогон можно продолжить со второго аккаунта — всё состояние (планы, handoff, ветки, память) локально и аккаунт-независимо. Порядок: новый чат → `/login` вторым аккаунтом → команда продолжения. ⚠️ Строго последовательно, НЕ параллельно на двух аккаунтах (один репо, один DerivedData — параллельные прогоны переломают сборку и git).
3. Приоритет при нехватке бюджета (сверху вниз режем хвост): отчёт+handoff > Ф1 > Ф2/Ф4 > Ф0-хвост > Ф3 > план 6b > реализация 6b. Т.е. реализация 6b — первый кандидат на выброс, целостный отчёт — неприкосновенен.
4. Каждая фаза перед стартом оценивается: «влезает ли фаза целиком в остаток окна?» Если нет — не начинать половину фазы, лучше остановиться на чистой границе.

**Рефакторинг и чистка (требование владельца):**
- По ходу каждой фазы: удалять замещённый код целиком (не комментировать, не оставлять `_old`/`Legacy`-копии) — уже закреплено условием 4 гейта Ф3; распространяется на все фазы.
- Ф2/Ф4: если по ходу видна мёртвая ветка в затронутых файлах — удалять в том же коммите с пометкой в отчёт.
- Большой рефакторинг вне скоупа фаз (декомпозиция FinanceDynamics и т.п.) — НЕ делать ночью, только зафиксировать наблюдения в `improvements/`.

## 2. Регламент (из ночного регламента 2026-07-05)

- Перед стартом: `awake` (caffeinate) — запускает владелец, проверить `pgrep caffeinate`.
- Бюджет на 2 окна: лёгкие фазы (Ф1, Ф2, Ф4, Ф0-хвост) — первым окном; тяжёлое (Ф3, план 6b) — последним.
- Реализация — только суб-агентами (Александр = sonnet, рутина/отчёты = haiku). Максимум 2 параллельно; фазы, трогающие Cashflow, — строго ПОСЛЕДОВАТЕЛЬНО (общие файлы).
- Коммит после каждой под-фазы (гейта). Сообщения — `feat(cashflow): …` по конвенции репо.
- **Без push. Без TestFlight. Без деплоя.** Только локальные ветки/мержи.
- Параллельные xcodebuild запрещены; если всё же нужны — изолировать `-derivedDataPath` (см. память 2026-07-05), вывод сборки не глушить grep'ом.
- Контекст: каждой фазе — свежий агент; координатор держит только статусы.

## 3. Гейты качества (каждая фаза, без исключений)

1. **Baseline ДО старта** (разово, первым шагом ночи): прогнать полный тестовый набор на чистом develop, зафиксировать список красных в `progress/2026-07-07-overnight-baseline.md`. Известные преэкзистентные: 4 recurring-теста `CashflowViewModelTests` + `FinanceLifecycleIntegrationTests.deletingMarketBuyRevertsSettlementAndPositionTogether`.
2. После каждой фазы: сборка (`xcodebuild build`) + прицельные тесты фазы + полный набор. Гейт = **ноль НОВЫХ красных** относительно baseline.
3. Self-audit по acceptance criteria фазы из базового плана — каждый пункт письменно ✅/❌ в план.
4. Impact analysis перед мержем в develop (Ф1/Ф2/Ф4/Ф0-хвост).
5. Скриншот-прогон на симуляторах: iPhone 16 Pro (основной) + малый экран (iPhone 16 / SE-класс) — минимум для Ф3 (риск №5 стресс-теста) и Ф1.
6. Новые строки UI — только через `Localizable.xcstrings` (RU/EN/zh-Hans), ноль raw-кириллицы (в UI уже 123 старых нарушения — не добавлять новых).
7. Актуализация базового плана после каждой фазы (статусы `[x]`, журнал).

## 4. Условия Ф3 (стресс-тест 2026-07-07, обязательные)

1. Черновики per-tab — в VM, не в `@State` вьюхи (переживают переоткрытие/memory warning).
2. Идемпотентность + anti-double-tap в `handle(.addTransaction)` (disable CTA после первого тапа).
3. Гейт по baseline (§3.1) — агент НЕ имеет права списывать новый красный тест на «преэкзистентное», не сверившись со списком baseline.
4. Единый экран — НОВЫМ файлом; `CashflowQuickEntrySheet.swift` и старый категорийный шит удаляются целиком (не комментируются); `CashflowOperationSheets.swift` (1518 строк) не должен вырасти.
5. **Ф3 не мержится в develop** — остаётся в `feature/cashflow-phase3-unified-sheet` до ручной проверки владельца утром.
6. Дефолт валюты = валюта выбранного счёта (чип); регресс-тесты на сумму+валюту для каждого таба; переиспользовать `CashflowCurrencySelectorView` (офлайн уже решён там), не писать новую конвертацию.
7. Dashboard FAB — не трогать (R1), но вручную проверить на симуляторе, что он открывает новую форму корректно; результат — в отчёт.

## 5. План 6b Путь B — требования к документу (задача №6)

Плановая сессия (без кода): инвентаризация всех легаси-файлов (Card/Credit/Investment миры, ~90 файлов — точный список), порядок сноса по слоям, миграция данных существующих счетов 1–2 юзеров в AccountsCore (через существующий `LegacyAccountConverter`), судьба 3 мостов (`AccountsCoreAdditionBridge`/`CashflowBridge`/`DepositCashflowBridge`) и `AccountTotalPolicy`, план отката, разбивка на фазы для sonnet-сессий. Учесть находки аудита 2026-07-07 (двоемирие = риск №1). Вход: `plans/2026-07-02__accounts-system-audit.md`, память `millio-6b-path-decision`.

## 6. Определение «готовое приложение к утру»

- develop: Ф1 + Ф2 + Ф4 + хвост Ф0 смержены, сборка зелёная, ноль новых красных тестов.
- `feature/cashflow-phase3-unified-sheet`: Ф3 готова, собрана, самопроверена, ЖДЁТ ручной проверки (чек-лист приложен к отчёту).
- Приложение установлено на симулятор iPhone 16 Pro (обе версии: develop и Ф3-ветка — вторая через отдельный derivedDataPath).
- `plans/2026-07-07__legacy-accounts-purge-path-b.md` готов; фаза 1 сноса реализована в `feature/legacy-accounts-purge` (или зафиксирован блокер/пропуск по бюджету).
- Ничего не запушено, TestFlight не собран (по команде утром — Nova).

## 7. Утренний отчёт (обязательный, формат TG)

В `progress/2026-07-08-overnight-report.md` + вывод в чат: по каждой фазе ✅/🔄/❌ план-vs-факт, список коммитов, результаты гейтов (тесты/сборка/скриншоты), чек-лист ручной проверки Ф3 (10 минут), блок «🔴 требует решения» (мерж Ф3, push, TestFlight). Канал TG для Millio не задан — сводку в чат, слать по команде.

## 8. Как запустить (для владельца)

**Схема ночи 2026-07-07 (владелец):** первый час владелец на связи — прогон стартует на основном аккаунте и работает на максимум; через ~час владелец переключается на второй аккаунт (`/logout` → `/login`), даёт команду продолжения и уходит — у прогона два лимита за ночь. Координатору: к моменту переключения держать чистую границу (закоммичено, handoff свеж).

Один новый чат в этой папке, **модель координатора — Fable** (`/model claude-fable-5[1m]`; если недоступен — opus), одна команда:

> **«Запусти ночной прогон по плану plans/2026-07-07__overnight-run.md»**

Если ночью прогон прервался (лимиты/сбой) — утром та же папка, команда: **«Продолжи ночной прогон по плану plans/2026-07-07__overnight-run.md с handoff'а»** (handoff: `progress/2026-07-08-overnight-handoff.md`).

Координатор сам: снимет baseline → поведёт фазы 1→2→4→Ф0-хвост→Ф3 через Александра (sonnet) последовательно → параллельно с ранними фазами (не с Ф3) закажет план 6b → после Ф3, если нет блокера и бюджет позволяет, — фаза 1 сноса 6b в отдельной ветке → соберёт билды → напишет отчёт. Перед уходом: выполнить `awake`, убедиться, что caffeinate жив (`pgrep caffeinate`).

## Журнал

- 2026-07-07: runbook создан (Fable), гейт Ф3 пройден стресс-тестом, скоуп ждёт подтверждения владельца.

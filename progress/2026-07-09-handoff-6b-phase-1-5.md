# Handoff 2026-07-09 (вечер): 6b Путь B → реализация Фазы 1.5 + Fable-ревью + подготовка к тестам

## Авторизация владельца (2026-07-09, явная)

Владелец: «в новом чате доделай план, потом fable проверь и будем тестить, сделай круто».
Расшифровка, согласованная контекстом сессии ревью:
1. **Реализуй Фазу 1.5 по плану** `plans/2026-07-07__legacy-accounts-purge-path-b.md` — guard phrase снята ИМЕННО для Ф1.5 (слияние моделей групп). Остальные фазы (Ф2–Ф6) — по-прежнему только по явной команде.
2. После реализации — **адверсариальное ревью на Fable** (главная сессия = Fable, либо ревью-субагент без model-override): корректность миграции, edge cases, потери данных, регрессии трёх экранов.
3. Затем — подготовка к **device-тестам**: свежий Release-билд на симулятор/устройство, чек-лист ручной проверки (Ф1 + Ф1.5 вместе), device stress-test перед мержем.

## Состояние репо

- **develop @ 016deec**, НЕ запушен (16 коммитов впереди origin). Push/TestFlight — только по явной команде.
- **Ветка `feature/legacy-accounts-purge`** — Фаза 1 (LegacyAccountsMigrator) реализована 2026-07-08, 11 тестов зелёные, НЕ мержена. Ф1.5 делать **в этой же ветке** поверх Ф1 (владелец подтвердил ранее: мерж Ф1 только после device stress-test — логично тестировать Ф1+Ф1.5 одним заходом).
- На симуляторе iPhone 17 Pro Max — старый билд 356ea02; Cashflow-фиксы (cfd1b3d) владелец руками не проверял.

## Задание: Фаза 1.5 — слияние моделей групп (план §4a, утверждён владельцем 2026-07-09)

**Полная спецификация фазы — в плане** `plans/2026-07-07__legacy-accounts-purge-path-b.md`, секция «4a» + «Фаза 1.5» (AC1–AC6). Ключевое:

- `AccountGroup` (`millio/Core/AccountsCore/AccountGroup.swift:6-17`) расширяется полями `isFavorite`, `usesManualAccountOrdering`, `priorityRaw` — **переносятся все три** (решение владельца).
- Новый одноразовый `GroupsMigrator` по паттерну `LegacyAccountsMigrator` (идемпотентность через постоянный маркер, не UserDefaults-only); переиспользует `AccountsCoreAdditionBridge.resolveAccountGroup` (:64-78), но переносит поля один раз.
- Три call site переключить на `AccountGroup`/`Account.group`:
  - `FinanceTotalsService.calculateGroupTotal` (`FinanceTotalsService.swift:148-160`)
  - `FinanceGroupService.orderedAccounts(for:)` (`FinanceGroupService.swift:83-88`)
  - `FinanceDynamicsViewModel.hydratedAccounts(in:)` (:2681-2695) + `updateDynamicsBreakdown()` (:1059-1086)
- Редактор группы (`FinanceGroupEditorView`) — на `AccountGroup`.
- `FinanceGroup.swift` НЕ удалять (уходит в Ф5); смешанные группы (легаси+ядро) должны суммироваться по обоим мирам (AC2) — пока легаси не снесена, читатели видят оба.
- Чинит баг владельца: «Вклады» 5 accounts — 0 ₽ / «No finance products» / «No groups» (диагноз: `plans/2026-07-05__unified-totals.md` §1.3a, верифицирован 2026-07-09).
- ⚠️ Схему НЕ версионировать в V6 в этой фазе, если добавление полей `AccountGroup` проходит lightweight (аддитивные optional/default-поля) — V6 отдана Ф5. Проверить фактически.
- AC6: обновить TODO-комментарий «ВРЕМЕННЫЙ мэппинг по имени (до Фазы 6…)» в `AccountsCoreAdditionBridge.swift`.

**Кастомные иконки групп — НЕ в этой фазе** (решение владельца: Фаза 6, редизайн Счетов).

## Регламент новой сессии

- Реализация — субагентом Александр (opus для архитектурной части или sonnet по фазам), верификация — отдельным субагентом; главное окно — оркестрация (правила 7–9 CLAUDE.md).
- Гейты: build 0 ошибок; полный `millioTests` — 0 новых красных vs baseline (18 известных, `progress/accounts-core-baseline-failures.md`); тесты AC1–AC4 фазы зелёные.
- Параллельные xcodebuild — изолировать DerivedData (`-derivedDataPath`), урок 2026-07-05.
- Коммиты на чистых границах в `feature/legacy-accounts-purge`; НЕ мержить, НЕ пушить без явного «да».
- После Fable-ревью: собрать Release-билд на симулятор, выдать владельцу чек-лист ручной проверки Ф1+Ф1.5 (миграция счетов, суммы групп, редактор группы, Groups-вкладка Динамики, идемпотентность повторного старта).
- `status.json` плана обновлять после фазы.

## Хвосты вне скоупа (не трогать, держать в поле зрения)

- Ф4 unified-totals (AccountsCore в заголовке Динамики) — может закрыться «бесплатно» в Ф2 Пути B.
- Сломанный тоггл `credit.includeInTotal` — отдельный тикет, уйдёт со сносом легаси-кредитов.
- Cashflow-фиксы cfd1b3d — ручная проверка владельцем не сделана (включить в тот же device-заход).

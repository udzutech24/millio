# Handoff: подготовка релиза millio 2.0 в App Store — 2026-09-14

## Решения владельца (14.09)
- Выпускаем 2.0 **без импорта банковских выписок** и **без AI**.
- Всё, что под вопросом, — **скрыть** в релизе, а не чинить сейчас.
- Название: **остаётся millio** (решение 14.09, переименование снято).

## Состояние на конец сессии
- `develop` (fac6685, запушен): перф скролла, экран вклада без налога, чек-лист первых шагов с переходами, мастер первого запуска отключён флагом (`AppLifecycleUseCase.swift:21`), строка «Быстрая настройка» удалена из профиля, затемнение сводки «Пока вас не было».
- TestFlight: 2.0 (build 4), проверен владельцем. Fastlane сам берёт номер сборки из ASC.
- Тесты: 2797 passed, регрессий нет (красные — исторические/order-effects).
- AI: 4 ветки не влиты (`feature/ai-*`), на сервере нет маршрутов и ANTHROPIC_API_KEY → в релиз не попадает само.
- Выписки: сервер `millio-back` крутит образ из ветки `agent/bank-statement-import`, которой нет в master; 10 коммитов паритета правил — в непушенной `feature/statement-rule-parity`. Любой передеплой сотрёт импорт. В релизе вход в «Банковская выписка» надо СКРЫТЬ в приложении.

## Что сделать в новой сессии (по порядку)
1. Скрыть в приложении (флагом, обратимо): импорт банковской выписки (экран «Импорт» → «Банковская выписка»), любые AI-входы, если что-то есть в develop. Инвентаризация «под вопросом» — кандидаты из памяти: экспорт платежа в календарь (гонка двух sheet + непереведённые ключи), расхождение баланса вклада список vs деталка, ревью-находки по кредиту на 0 ₽ и архивным счетам (не подтверждены).
2. ASC (Nova): привязать build к версии 2.0; «Что нового» во всех 7 локалях (5 локалей до сих пор про 1.9); скриншоты устарели (15.08, до редизайна), de/es/fr без скриншотов; руками проверить App Privacy (без выписок транзита данных нет — актуализировать), возрастной рейтинг, цены, подписки PRO и привязку к версии, заметки ревьюеру.
3. Тексты сайта/политики: убрать упоминания выписок и AI, если есть.
4. Новая сборка → TestFlight → device-проверка → подача на ревью только по «да».

## Открытые вопросы владельцу
- Вход через Apple или гостевой режим на телефоне (13.09) — неактуально, если выписки скрываем.
- Ветки `fix/release-2.0-blockers`, `feature/first-steps-card` + worktree — снести? (влиты в develop)

## Правила, всплывшие в сессии
- Симуляторы: своих устройств агентам не создавать, не больше 3 загруженных, за собой убирать (в CLAUDE.md).
- iPhone владельца ставится поверх только через devicectl; TestFlight-сборка поверх dev-сборки не встаёт без удаления.

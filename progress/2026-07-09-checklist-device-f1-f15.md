# Чек-лист ручной проверки: 6b Ф1 + Ф1.5 (Release-билд, симулятор iPhone 17 Pro Max)

Ветка `feature/legacy-accounts-purge` @ 369c57c (Ф1 = LegacyAccountsMigrator, Ф1.5 = слияние групп + фикс ревью).

**Решение владельца 2026-07-09: проверяем ЧИСТУЮ установку на iPhone 17 Pro Max iOS 26.5 (билд уже установлен и запущен).** Миграция поверх существующих данных — отложена до реального устройства; секции A и B3 («до/после») на чистом сторе неприменимы — вместо них секция A′. Бэкап майского стора симулятора 26.4 сохранён в `millio-dev/.sim-backups/2026-07-09-9A811413/` на случай, если захочется прогнать полный сценарий позже.

## A′. Чистый старт (вместо A на этом заходе)

- [ ] **A′1. Первый старт** — приложение стартует без краша, пустое состояние корректное (нет «мусорных» групп/счетов).
- [ ] **A′2. Создание счетов** — создай 2–3 новых счёта разных типов (карта, вклад, инвестиция) — балансы и типы корректны.
- [ ] **A′3. Создание группы** — создай группу, положи в неё новые счета — сумма группы = сумме счетов.

## A. Миграция счетов (Ф1) — ⏸ отложено до реального устройства (чистый стор, мигрировать нечего)

- [ ] **A1. Первый старт после обновления** — приложение стартует без краша, без зависания на сплэше.
- [ ] **A2. Общий баланс не изменился** — сумма на Дашборде и в «Счетах» ровно та же, что до обновления (запиши цифру ДО установки).
- [ ] **A3. Все счета на месте** — количество и состав счетов в списке «Счета» прежние, ни один не пропал и не задвоился.
- [ ] **A4. Балансы отдельных счетов** — выборочно 2–3 счёта (карта, кредит, инвестиция): баланс совпадает с прежним.

## B. Группы (Ф1.5 — главное)

- [ ] **B1. Баг «Вклады = 0 ₽» ушёл** — группа, целиком из новых (AccountsCore) счетов, показывает верную сумму и список продуктов (раньше: 0 ₽ / «No finance products»).
- [ ] **B2. Смешанные группы** — ⏸ на чистом сторе легаси-счетов нет; проверить на устройстве.
- [ ] **B3. Атрибуты групп** — на чистой установке: выставь ⭐/цвет/порядок у созданной группы → перезапуск → сохранились. («До/после миграции» — на устройстве.)
- [ ] **B4. Groups-вкладка Динамики** — в breakdown Analytics/Динамики группы из новых счетов присутствуют и с ненулевыми цифрами; «No groups» не показывается, если группы есть.

## C. Редактор группы (Ф1.5)

- [ ] **C1. Открытие** — редактор любой группы открывается, поля заполнены текущими значениями.
- [ ] **C2. Правка** — смени имя/цвет/избранность → сохранение → изменения видны в списке «Счета» И в Динамике (breakdown).

## D. Идемпотентность (Ф1 + Ф1.5)

- [ ] **D1. Повторный старт** — полностью убить приложение (свайп из app switcher) и запустить снова: балансы/группы/атрибуты не изменились, дублей не появилось.
- [ ] **D2. Ещё раз** — третий запуск: то же самое (миграции — no-op).

## E. Хвост: Cashflow-фиксы cfd1b3d (не проверялись с 2026-07-08)

- [ ] **E1. FAB Доход/Расход** — открывается unified-контейнер (не старый флоу).
- [ ] **E2. Выбор категории** — ведёт сразу в полный эдитор транзакции.

## Если что-то сломалось

- Откат: миграция Ф1 обратима (`unconvert`), Ф1.5 не удаляет легаси-данные (`FinanceGroup` живёт до Ф5). Ветка НЕ мержена — develop чист.
- Зафиксируй: какой пункт, экран, что ожидал/что увидел — и неси в чат.

**После прохождения чек-листа**: device stress-test → явное «да» владельца → только потом мерж в develop.
